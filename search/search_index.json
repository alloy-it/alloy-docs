{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"","title":"Alloy","text":"<p>Reproducible build environments for embedded software teams.</p> <p>Setting up a cross-compilation toolchain shouldn't take two days. Onboarding a new developer shouldn't mean walking them through a wall of manual steps. And \"it builds on my machine\" is not an acceptable answer.</p> <p>Alloy solves this by letting you define your build environment as code: a single blueprint that anyone on your team can use to spin up an identical, isolated VM in minutes. Define your environment once; others can reuse it with one command. No README archaeology, no copy-paste, no \"it works on my machine.\"</p>"},{"location":"#how-it-works","title":"How it works","text":"Step 1: Install alloy-provisionerStep 2: Pick a blueprint from Alloy HubStep 3: Install the environmentStep 4: Start building <p>Download and install the alloy-provisioner binary on any Linux machine, VM, or Docker container.</p> <pre><code># Download the latest release (amd64)\nwget https://github.com/alloy-it/alloy-provisioner-releases/releases/latest/download/alloy-provisioner_linux_amd64.deb\nsudo dpkg -i alloy-provisioner_linux_amd64.deb\n</code></pre> <p>Browse alloy-it.io or search locally to find a ready-made environment for your toolchain.</p> <pre><code>alloy-host catalog update\nalloy-host catalog search arm\n# Find the ID you need, e.g. community/arm-none-eabi\n</code></pre> <p>One command installs the full build environment: packages, toolchain, env vars, everything.</p> <pre><code>sudo alloy-provisioner install community/arm-none-eabi\n</code></pre> <p>The provisioner pulls the blueprint from Alloy Hub, verifies all SHA256 checksums, and runs each task in order. Re-runs are safe; completed tasks are skipped.</p> <p>Your tools are installed and your environment is configured. Source the env file and build.</p> <pre><code>source /etc/alloy/env\narm-none-eabi-gcc --version\ncmake --version\nmake\n</code></pre>"},{"location":"#what-you-get","title":"What you get","text":"<p>A defined, versioned build environment: not a README full of instructions that slowly goes out of date.</p> <p>Isolation: your Zephyr project and your FreeRTOS project live in separate VMs. Upgrading one doesn't break the other.</p> <p>Hardware access: USB passthrough lets you flash and debug real hardware directly from the VM.</p> <p>Reproducibility: a lockfile records the exact version of every tool. Run it six months later, on any machine, and get the same result.</p> <p>Same definition everywhere: one blueprint for local dev and CI; no separate setup docs or divergent tool versions.</p>"},{"location":"#who-its-for","title":"Who it's for","text":"<p>Alloy is built for embedded software developers who are tired of:</p> <ul> <li>Spending the first day of a new project setting up their toolchain</li> <li>Onboarding a new team member with a multi-page setup doc</li> <li>Breaking their environment when they upgrade an SDK</li> <li>Dealing with \"works on my machine\" between macOS, Linux, and Windows hosts</li> </ul>"},{"location":"#ready-to-start","title":"Ready to start?","text":"<ul> <li> <p> Get started in 15 minutes</p> <p>Install Alloy Host, create your first environment, and SSH in.</p> </li> <li> <p> Browse the catalog</p> <p>Use a ready-made blueprint for popular embedded toolchains.</p> </li> <li> <p> Concepts</p> <p>How blueprints, the catalog, and Alloy Host work together.</p> </li> <li> <p> Why Alloy-it?</p> <p>Not sure if you need this? See what problem it solves.</p> </li> </ul>"},{"location":"concepts/","title":"How Alloy Works","text":"<p>This page gives you the full mental model: what a blueprint is, how alloy-provisioner installs it, where Alloy Hub fits in, and what alloy-host adds on top.</p>"},{"location":"concepts/#blueprint","title":"Blueprint","text":"<p>A blueprint is a small directory of YAML files that completely defines a build environment. It declares which tools to install, which versions, where they come from, and how to configure them.</p> <p>The same blueprint runs everywhere:</p> <ul> <li>Natively on Linux: run alloy-provisioner directly on a bare-metal machine, a test rack, or any existing Linux VM.</li> <li>In a managed VM: alloy-host spins up a VM (VirtualBox, WSL2, or Docker) and runs the provisioner inside it.</li> <li>In a Docker image: embed alloy-provisioner in a Dockerfile to build reproducible CI base images.</li> <li>On CI runners: the exact same blueprint and lockfile give your pipeline the same environment as every developer.</li> </ul> <p>Blueprints are version-controlled and committed alongside source code. Update the toolchain by changing the blueprint; everyone re-runs the provisioner and gets the same result.</p>"},{"location":"concepts/#alloy-provisioner","title":"alloy-provisioner","text":"<p>alloy-provisioner is the core engine. It is the tool that actually installs the environment.</p> <p>It runs inside any Linux system (bare metal, VM, WSL2, or Docker container) and:</p> <ul> <li>Reads the blueprint (manifest + task files) and the lockfile.</li> <li>Executes each task in the declared order: install packages, download toolchains, write env files, run custom commands.</li> <li>Uses three layers of idempotency so re-runs skip work that is already done.</li> <li>Can pull a blueprint directly from Alloy Hub or use a local directory.</li> </ul> <p>You can invoke alloy-provisioner directly:</p> <pre><code># Pull a blueprint from Alloy Hub and provision\nalloy-provisioner install community/arm-none-eabi\n\n# Provision from a local blueprint directory\nalloy-provisioner --blueprint-dir /path/to/blueprint\n</code></pre> <p>This is the same binary that alloy-host installs and runs inside VMs and containers.</p>"},{"location":"concepts/#alloy-hub","title":"Alloy Hub","text":"<p>Alloy Hub (alloy-it.io) is the public registry for community-maintained blueprints. Browse and search for ready-made environments covering common embedded toolchains, SDKs, and development tools.</p> <p>You can also browse the catalog locally with alloy-host:</p> <pre><code>alloy-host catalog update\nalloy-host catalog search arm\nalloy-host catalog info toolchain.arm-gnu.arm-none-eabi@stable\n</code></pre> <p>Blueprints published to Alloy Hub are backed by the alloy-catalog repository on GitHub. The catalog stores metadata (IDs, versions, download URLs, and SHA256 checksums), not binaries.</p>"},{"location":"concepts/#alloy-host","title":"alloy-host","text":"<p>alloy-host is an optional helper tool for developer workstations. It does not provision environments itself. It manages the lifecycle of local VMs and Docker containers so that alloy-provisioner can run inside them.</p> <p>Use alloy-host when you want to:</p> <ul> <li>Isolate your build environment from your host machine.</li> <li>Switch between multiple environments for different projects.</li> <li>Test a blueprint before publishing, without manually setting up a VM.</li> </ul> <p>alloy-host supports three backends:</p> Backend Platforms Use case VirtualBox + Vagrant Windows, macOS, Linux Default; full VM isolation WSL2 Windows Fast Linux environments on Windows Docker All platforms Lightweight containers <p>Note</p> <p>Any VM managed by your own tools (Vagrant, VirtualBox, Proxmox, etc.) can run alloy-provisioner natively once it has a Linux OS. alloy-host is a convenience wrapper, not a requirement.</p>"},{"location":"concepts/#catalog","title":"Catalog","text":"<p>The catalog is a versioned metadata registry for toolchains and SDKs. It stores IDs, versions, download URLs, and SHA256 checksums per host architecture, not the binaries themselves.</p> <p>How blueprints use the catalog</p> <p>Blueprints declare which tools they need using refs (e.g. <code>toolchain.arm-gnu.arm-none-eabi@stable</code>). Running <code>alloy-host resolve</code> turns those refs into concrete URLs and checksums and writes <code>alloy.lock.yml</code> next to the blueprint. The provisioner uses the lockfile to download the exact artifacts, reproducibly.</p> <p>Why it scales</p> <ul> <li>One catalog entry is maintained once and reused by any number of blueprints and teams.</li> <li>Updating a toolchain means updating the catalog entry and re-resolving; everyone gets the same artifact with no copy-pasting of URLs.</li> </ul>"},{"location":"concepts/#architecture-at-a-glance","title":"Architecture at a glance","text":"<pre><code>flowchart TD\n    HUB[\"Alloy Hub\\nalloy-it.io\"]\n    CAT[\"Catalog\\n~/.alloy-it/catalog\"]\n    BP[\"Blueprint\\nmanifest + task files\"]\n    LOCK[\"alloy.lock.yml\"]\n    PROV[\"alloy-provisioner\"]\n    HOST[\"alloy-host\\n(optional helper)\"]\n\n    HUB --&gt;|pull blueprint| PROV\n    HUB --&gt;|backs| CAT\n    CAT --&gt;|resolve refs| LOCK\n    LOCK --&gt; BP\n    BP --&gt; PROV\n\n    HOST --&gt;|manages lifecycle| VM[\"VM / WSL2 / Docker\\n(runs alloy-provisioner)\"]\n\n    PROV --&gt;|installs into| ENV1[\"Linux (bare metal)\"]\n    PROV --&gt;|installs into| ENV2[\"VM / WSL2\"]\n    PROV --&gt;|installs into| ENV3[\"Docker image\"]</code></pre>"},{"location":"concepts/#common-workflows","title":"Common workflows","text":"Goal Where to go Install an environment (native Linux, VM, or Docker) Get Started Use alloy-host (spin up a managed VM on your workstation) With Alloy Host Bake a blueprint into a Docker image In a Docker Image Write your own blueprint (from scratch or extending from Alloy Hub) Developing Blueprints Publish to Alloy Hub (open a PR to alloy-catalog) Publishing to Alloy Hub Browse toolchains (ARM GCC, Nordic nRF, Go, and more) Available Toolchains"},{"location":"concepts/#next-steps","title":"Next steps","text":"<ul> <li>Choose your installation path</li> <li>Browse blueprints on Alloy Hub</li> <li>Write your own blueprint</li> </ul>"},{"location":"why-alloy/","title":"Why Alloy-it?","text":""},{"location":"why-alloy/#the-problem-with-embedded-dev-environments","title":"The problem with embedded dev environments","text":"<p>Setting up a build environment for embedded software is one of the most frustrating parts of the job. It's not a skill issue; it's that there's no standard way to do it, and every project has a slightly different setup.</p> <p>Here's what a typical first day on a new embedded project looks like:</p> <ol> <li>Clone the repo</li> <li>Read the README: it says \"install ARM GCC 12.3 and the vendor SDK\"</li> <li>Install ARM GCC, but Homebrew gives you 13.2, not 12.3</li> <li>Find the right download for your OS from the vendor site</li> <li>Set up PATH, check it's the right version</li> <li>Install the SDK, but it depends on Python 3.10 and your system has 3.12</li> <li>Install Python 3.10 alongside your existing Python; now you have three</li> <li>Follow the vendor's SDK setup guide, written for Ubuntu 20.04, but you're on macOS</li> <li>Run <code>cmake ..</code> and hit a missing dependency</li> <li>Search Stack Overflow for the next two hours</li> <li>It finally works, but your colleague hits a different error because they're on Windows</li> </ol> <p>This is not exceptional. This is Tuesday.</p>"},{"location":"why-alloy/#what-makes-it-hard","title":"What makes it hard","text":"<p>The core issue is that embedded toolchains have hard version dependencies that don't always align with what your system package manager provides. The ARM GCC compiler, the vendor SDK, the required Python version, the right version of CMake: they all have to match, and the window of acceptable versions is narrow.</p> <p>Beyond that:</p> <p>Toolchains are large and opinionated. They install files in global locations, set environment variables, and sometimes conflict with other projects on the same machine.</p> <p>Documentation drifts. A README written for Ubuntu 20.04 isn't tested on macOS Sonoma. By the time someone finds the gap, it's already caused a day of lost work.</p> <p>New team members suffer most. An experienced developer can debug a broken toolchain setup. A new hire trying to get their first build working is staring at opaque errors with no context.</p> <p>CI/CD diverges from local. You get it working on your machine, then discover CI is using different tool versions. You fix CI, break your local. This cycle is a productivity killer. With Alloy, one blueprint fixes that; local and CI use the same definition.</p>"},{"location":"why-alloy/#how-alloy-solves-it","title":"How Alloy solves it","text":"<p>Alloy separates defining an environment from using one.</p> <p>You write a blueprint, a single universal declaration that says exactly what your environment needs: which tools, which versions, where they come from, how they're configured. This blueprint lives in your repository alongside your source code. The same blueprint runs on local VMs, on servers, and in CI: same file, same lockfile, same result.</p> <p>When anyone on your team runs <code>alloy-host up</code>, Alloy:</p> <ol> <li>Creates an isolated Linux VM on their machine</li> <li>Reads the blueprint</li> <li>Downloads and installs exactly the tools specified</li> <li>Configures environment variables and paths</li> </ol> <p>The result is a running VM where everything is already set up. SSH in and build.</p> <p>The blueprint is versioned in git. When you update the toolchain, you update the blueprint, commit it, and your teammates pick up the change with a <code>git pull</code> + <code>alloy-host up</code>. No manual steps, no README updates, no \"did you upgrade yet?\" conversations.</p>"},{"location":"why-alloy/#isnt-this-what-docker-is-for","title":"Isn't this what Docker is for?","text":"<p>Docker is a great tool, but it makes specific trade-offs that don't always fit embedded development.</p> Docker Alloy USB device access Limited, host-dependent First-class support Vendor SDK support Often incompatible Full Linux environment Windows host support Requires WSL2 tuning Native via WSL2 or VirtualBox Persistent workspace Volume mounts Dedicated persistent disk Environment isolation Process-level Full VM isolation Familiar to embedded devs Sometimes Always (it's just a VM) <p>USB passthrough in particular is a consistent pain point with Docker. Alloy treats hardware access as a first-class feature: you can attach your target board to the VM and flash firmware without leaving your development environment.</p>"},{"location":"why-alloy/#who-is-alloy-for","title":"Who is Alloy for?","text":"<p>Individual developers who are tired of maintaining a mess of installed tools and want clean, project-specific environments they can throw away and rebuild.</p> <p>Teams who want everyone to use the same toolchain without spending time on environment support.</p> <p>Companies who need to reproduce a build environment exactly (same compiler, same SDK, same everything) for compliance, debugging, or long-term support of deployed firmware.</p> <p>Open source projects that want contributors to get a consistent build environment with minimal friction: one blueprint, one command, no setup docs to maintain.</p> <p>If you've ever said \"it builds on my machine\" or spent more than an hour setting up a toolchain, Alloy is for you.</p> <p>Get started \u2192</p>"},{"location":"blueprints/","title":"Developing Blueprints","text":"<p>A blueprint is a small directory of YAML files that completely defines a build environment. Write it once, use it anywhere: natively on Linux, in a VM, in a Docker image, on CI.</p>"},{"location":"blueprints/#what-a-blueprint-contains","title":"What a blueprint contains","text":"<pre><code>my-arm-env/\n\u251c\u2500\u2500 manifest.yml        # required: name, version, variables, task order\n\u251c\u2500\u2500 variables.yml       # optional: shared variable definitions\n\u251c\u2500\u2500 00-system.yml       # task file: base OS packages\n\u251c\u2500\u2500 10-toolchain.yml    # task file: ARM GCC toolchain\n\u251c\u2500\u2500 20-environment.yml  # task file: env vars and config\n\u2514\u2500\u2500 alloy.lock.yml      # generated: pinned URLs and SHA256 checksums\n</code></pre> <p>The provisioner reads <code>manifest.yml</code> to know the order, then executes each task file in sequence. Task files are independent, idempotent, and re-runnable.</p>"},{"location":"blueprints/#two-ways-to-get-started","title":"Two ways to get started","text":"<ul> <li> <p> Extend from Alloy Hub</p> <p>Start with a community blueprint from Alloy Hub and customize it for your project. The fastest path if a similar environment already exists.</p> <p>Extend from Alloy Hub \u2192</p> </li> <li> <p> Write from scratch</p> <p>Define your environment from the ground up. Follow the step-by-step guide to create a manifest, write task files, and test locally.</p> <p>Write from scratch \u2192</p> </li> </ul>"},{"location":"blueprints/#the-blueprint-development-lifecycle","title":"The blueprint development lifecycle","text":"<pre><code>flowchart LR\n    Write[\"Write / extend blueprint\"] --&gt; Test[\"Test locally\\n(alloy-host or native)\"]\n    Test --&gt; Fix[\"Fix issues\"]\n    Fix --&gt; Test\n    Test --&gt; Resolve[\"Resolve lockfile\\nalloy-host resolve\"]\n    Resolve --&gt; Commit[\"Commit blueprint\\n+ lockfile\"]\n    Commit --&gt; Publish[\"Open PR to alloy-catalog\\n\u2192 published on Alloy Hub\"]</code></pre> <ol> <li>Write your manifest and task files.</li> <li>Test by provisioning into a local VM or running alloy-provisioner natively.</li> <li>Resolve the lockfile to pin toolchain URLs and checksums.</li> <li>Commit the blueprint and lockfile to your repo.</li> <li>Publish by opening a PR to the alloy-catalog repo; automated checks run, and on merge it appears on Alloy Hub.</li> </ol>"},{"location":"blueprints/#in-this-section","title":"In this section","text":"Page What you'll learn Blueprint Structure manifest.yml, task files, run_order, lockfile Variables &amp; Configuration Manifest vars, variables.yml, .env files, required_env The Layers Approach How to split a blueprint into maintainable layers Extend from Alloy Hub Start from an existing blueprint and customize Write from Scratch Step-by-step: manifest \u2192 task files \u2192 test \u2192 publish Publishing to Alloy Hub PR to alloy-catalog \u2192 availability on Alloy Hub"},{"location":"blueprints/extending/","title":"Extend from Alloy Hub","text":"<p>The fastest way to write a blueprint is to start with one that already exists on Alloy Hub and customize it for your project. This is the recommended approach when a similar environment already exists.</p>"},{"location":"blueprints/extending/#1-find-a-blueprint-on-alloy-hub","title":"1. Find a blueprint on Alloy Hub","text":"<p>Browse alloy-it.io in your browser, or search the catalog locally:</p> <pre><code>alloy-host catalog update\nalloy-host catalog search arm\nalloy-host catalog info toolchain.arm-gnu.arm-none-eabi@stable\n</code></pre> <p>Or use alloy-provisioner to list available blueprints:</p> <pre><code>alloy-provisioner clone --list community\n</code></pre>"},{"location":"blueprints/extending/#2-clone-the-blueprint","title":"2. Clone the blueprint","text":"<p>Download a blueprint from Alloy Hub to a local directory:</p> <pre><code>alloy-provisioner clone community/arm-none-eabi --output ./my-blueprint\n</code></pre> <p>This creates a <code>my-blueprint/</code> directory with the manifest, task files, and lockfile from the hub.</p> <p>Alternatively, if the blueprint is in the alloy-catalog GitHub repo, you can clone the relevant directory directly:</p> <pre><code># Find the blueprint directory in alloy-catalog\ngit clone https://github.com/alloy-it/alloy-catalog.git\ncp -r alloy-catalog/blueprints/arm-gnu/arm-none-eabi ./my-blueprint\n</code></pre>"},{"location":"blueprints/extending/#3-understand-the-existing-structure","title":"3. Understand the existing structure","text":"<p>Read the manifest to understand what the blueprint does:</p> <pre><code>cat my-blueprint/manifest.yml\n</code></pre> <p>Typical structure (following the layers approach):</p> <pre><code>my-blueprint/\n\u251c\u2500\u2500 manifest.yml        # name, version, variables, run_order\n\u251c\u2500\u2500 00-system.yml       # base OS packages\n\u251c\u2500\u2500 10-toolchain.yml    # toolchain download and install\n\u251c\u2500\u2500 20-environment.yml  # PATH and env vars\n\u2514\u2500\u2500 alloy.lock.yml      # pinned URLs and SHAs\n</code></pre> <p>Read each task file to understand what is being installed and where. ; these control installation paths and versions.</p>"},{"location":"blueprints/extending/#4-customize-for-your-project","title":"4. Customize for your project","text":"<p>Add packages to an existing layer:</p> <pre><code># 00-system.yml \u2014 add after existing tasks\n- name: Install project-specific tools\n  action: apt_install\n  packages:\n    - libssl-dev\n    - doxygen\n    - cppcheck\n</code></pre> <p>Override a variable by editing <code>manifest.yml</code>:</p> <pre><code>variables:\n  ARM_TOOLCHAIN_DEST: /opt/my-project/arm-gcc   # override the default path\n  DEV_USERNAME: ubuntu                           # override default username\n</code></pre> <p>Add a new layer for project-specific setup:</p> <p>Create <code>30-project.yml</code>:</p> <pre><code>- name: Install J-Link udev rules\n  action: udev_rules\n  rule_file: /etc/udev/rules.d/99-jlink.rules\n  content: |\n    SUBSYSTEM==\"usb\", ATTRS{idVendor}==\"1366\", MODE=\"0666\", GROUP=\"plugdev\"\n\n- name: Install pyOCD\n  action: python_package\n  package: pyocd\n  version: \"0.36.0\"\n  creates: /usr/local/bin/pyocd\n</code></pre> <p>Add it to <code>run_order:</code> in <code>manifest.yml</code>:</p> <pre><code>run_order:\n  - 00-system.yml\n  - 10-toolchain.yml\n  - 20-environment.yml\n  - 30-project.yml     # add your new layer\n</code></pre> <p>Update the version:</p> <pre><code>name: ARM Cortex-M \u2014 My Project\nversion: 1.1.0          # bump from the original\ndescription: ARM GCC toolchain with project-specific tools for MyProject.\n</code></pre>"},{"location":"blueprints/extending/#5-test-locally","title":"5. Test locally","text":"<p>Test your customized blueprint by provisioning into a local VM with alloy-host:</p> <pre><code>alloy-host init test-env --blueprint ./my-blueprint\ncd test-env\nalloy-host up\nalloy-host ssh\n</code></pre> <p>Or natively on a Linux machine:</p> <pre><code>sudo alloy-provisioner --blueprint-dir ./my-blueprint\n</code></pre> <p>Verify that your additions work as expected. If something fails, fix the task file and re-run; completed tasks are skipped, so only the failed (and new) tasks execute.</p>"},{"location":"blueprints/extending/#6-update-the-lockfile","title":"6. Update the lockfile","text":"<p>If you added or changed any <code>toolchains:</code> catalog refs in <code>manifest.yml</code>, regenerate the lockfile:</p> <pre><code>alloy-host resolve\n</code></pre> <p>This writes a new <code>alloy.lock.yml</code> with resolved URLs and SHA256 checksums.</p> <p>If you only added apt packages or local downloads with explicit <code>sha256:</code> fields, you do not need to re-resolve.</p>"},{"location":"blueprints/extending/#7-commit","title":"7. Commit","text":"<pre><code>git add manifest.yml 00-system.yml 30-project.yml alloy.lock.yml\ngit commit -m \"feat: extend arm-none-eabi blueprint with project tools\"\n</code></pre> <p>Commit the blueprint directory and the lockfile. Do not commit <code>alloy.state.yml</code> or <code>.env</code>.</p>"},{"location":"blueprints/extending/#next-steps","title":"Next steps","text":"<ul> <li>Sharing with your team: give teammates a one-command setup</li> <li>Publishing to Alloy Hub: contribute your blueprint back to the community</li> <li>Variables &amp; Configuration: environment files and required variables</li> </ul>"},{"location":"blueprints/from-scratch/","title":"Write a Blueprint from Scratch","text":"<p>This guide walks through writing a complete ARM Cortex-M blueprint from scratch: manifest, task files (using the layers approach), and testing locally before publishing.</p>"},{"location":"blueprints/from-scratch/#what-well-build","title":"What we'll build","text":"<p>An ARM Cortex-M development environment with:</p> <ul> <li>Base OS packages (cmake, ninja, git, python3)</li> <li>ARM GCC 13.3 cross-compilation toolchain</li> <li>PATH and CMake toolchain file configuration</li> </ul> <p>By the end you'll have a working blueprint that can provision a native Linux machine, a VM, or a Docker container.</p>"},{"location":"blueprints/from-scratch/#step-1-create-the-directory","title":"Step 1: Create the directory","text":"<pre><code>mkdir my-arm-env &amp;&amp; cd my-arm-env\n</code></pre>"},{"location":"blueprints/from-scratch/#step-2-write-manifestyml","title":"Step 2: Write <code>manifest.yml</code>","text":"<pre><code>name: ARM Cortex-M Development Environment\nversion: 1.0.0\ndescription: ARM GCC 13.3 toolchain for Cortex-M embedded development.\n\nvariables:\n  ARM_TOOLCHAIN_DEST: /opt/arm-none-eabi\n  DEV_USERNAME: vagrant\n\nrun_order:\n  - 00-system.yml\n  - 10-toolchain.yml\n  - 20-environment.yml\n</code></pre> <p>Start with the minimum required fields: <code>name</code>, <code>version</code>, and <code>run_order</code>. Add <code>variables:</code> for any value that appears in more than one task file, or that you might want to override.</p>"},{"location":"blueprints/from-scratch/#step-3-write-variablesyml-optional","title":"Step 3: Write <code>variables.yml</code> (optional)","text":"<p>For larger blueprints, move variable definitions to a separate file to keep the manifest clean:</p> <pre><code># variables.yml\nARM_TOOLCHAIN_DEST: /opt/arm-none-eabi\nDEV_USERNAME: vagrant\nTOOLCHAIN_VERSION: \"13.3.rel1\"\n</code></pre> <p>If you use <code>variables.yml</code>, you can remove the same keys from <code>manifest.yml variables:</code>. The manifest always takes precedence on conflict.</p>"},{"location":"blueprints/from-scratch/#step-4-write-the-system-layer-00-systemyml","title":"Step 4: Write the system layer (<code>00-system.yml</code>)","text":"<pre><code>- name: Update APT cache\n  action: apt_install\n  packages: []\n  always_run: true\n\n- name: Install build essentials\n  action: apt_install\n  packages:\n    - git\n    - cmake\n    - ninja-build\n    - python3\n    - wget\n    - curl\n    - ca-certificates\n    - xz-utils\n    - gdb-multiarch\n</code></pre>"},{"location":"blueprints/from-scratch/#step-5-write-the-toolchain-layer-10-toolchainyml","title":"Step 5: Write the toolchain layer (<code>10-toolchain.yml</code>)","text":"<p>Download and extract the ARM GCC toolchain. Use <code>per_arch</code> to support both <code>amd64</code> and <code>arm64</code> host machines:</p> <pre><code>- name: Install ARM GCC 13.3 toolchain\n  action: unarchive_from_url\n  dest: \"{{.Vars.ARM_TOOLCHAIN_DEST}}\"\n  strip_components: 1\n  creates: \"{{.Vars.ARM_TOOLCHAIN_DEST}}/bin/arm-none-eabi-gcc\"\n  per_arch:\n    amd64:\n      url: https://developer.arm.com/-/media/Files/downloads/gnu/13.3.rel1/binrel/arm-gnu-toolchain-13.3.rel1-x86_64-arm-none-eabi.tar.xz\n      sha256: \"aaabbbccc111222333444555666777888999000aaabbbccc111222333444555666\"\n    arm64:\n      url: https://developer.arm.com/-/media/Files/downloads/gnu/13.3.rel1/binrel/arm-gnu-toolchain-13.3.rel1-aarch64-arm-none-eabi.tar.xz\n      sha256: \"dddeeefff111222333444555666777888999000dddeeefff111222333444555666\"\n</code></pre> <p>SHA256 is mandatory</p> <p>Always include <code>sha256</code> on every download. The provisioner verifies the hash and fails if it does not match. This is what makes blueprints reproducible: anyone who runs this blueprint gets byte-for-byte identical toolchain binaries.</p> <p>To find the SHA256:</p> <pre><code>sha256sum arm-gnu-toolchain-13.3.rel1-x86_64-arm-none-eabi.tar.xz\n</code></pre>"},{"location":"blueprints/from-scratch/#step-6-write-the-environment-layer-20-environmentyml","title":"Step 6: Write the environment layer (<code>20-environment.yml</code>)","text":"<p>Configure the shell environment and generate a CMake toolchain file:</p> <pre><code>- name: Add ARM GCC to PATH\n  action: write_env_file\n  file: /etc/profile.d/10-arm-toolchain.sh\n  content: |\n    export PATH=$PATH:{{.Vars.ARM_TOOLCHAIN_DEST}}/bin\n  owner: root:root\n  mode: \"0644\"\n\n- name: Generate CMake toolchain file\n  action: cmake_toolchain\n  file: /opt/cmake/arm-none-eabi.cmake\n  target_system: Generic\n  target_processor: ARM\n  compiler_prefix: arm-none-eabi-\n  compiler_path: \"{{.Vars.ARM_TOOLCHAIN_DEST}}/bin\"\n  owner: root:root\n  mode: \"0644\"\n</code></pre>"},{"location":"blueprints/from-scratch/#step-7-validate-the-blueprint","title":"Step 7: Validate the blueprint","text":"<p>Check for syntax errors before provisioning:</p> <pre><code>alloy-host validate ./my-arm-env\n</code></pre> <p>This catches missing required fields, unknown action types, and YAML syntax errors without starting a VM.</p>"},{"location":"blueprints/from-scratch/#step-8-test-by-provisioning","title":"Step 8: Test by provisioning","text":"<p>With alloy-host (recommended):</p> <pre><code>alloy-host init test-env --blueprint ./my-arm-env\ncd test-env\nalloy-host up\n</code></pre> <p>Watch the provisioner execute each task. If a task fails, fix the issue and re-run <code>alloy-host provision</code>; completed tasks are skipped.</p> <p>Natively on a Linux machine:</p> <pre><code>sudo alloy-provisioner --blueprint-dir ./my-arm-env\n</code></pre>"},{"location":"blueprints/from-scratch/#step-9-verify-inside-the-environment","title":"Step 9: Verify inside the environment","text":"<pre><code>alloy-host ssh\n\n# Inside the VM:\narm-none-eabi-gcc --version\n# arm-none-eabi-gcc (Arm GNU Toolchain 13.3.Rel1) 13.3.1\n\ncmake --version\nninja --version\nls /opt/cmake/arm-none-eabi.cmake\n</code></pre> <p>Try building a real project:</p> <pre><code>cd /vagrant/my-project\ncmake -GNinja -DCMAKE_TOOLCHAIN_FILE=/opt/cmake/arm-none-eabi.cmake -B build\nninja -C build\n</code></pre>"},{"location":"blueprints/from-scratch/#step-10-resolve-the-lockfile-if-using-catalog-refs","title":"Step 10: Resolve the lockfile (if using catalog refs)","text":"<p>If you referenced catalog toolchains in <code>manifest.yml toolchains:</code>, generate the lockfile:</p> <pre><code>alloy-host resolve\n</code></pre> <p>If you specified all URLs and SHA256 values directly in task files (as in this guide), you do not need a lockfile.</p>"},{"location":"blueprints/from-scratch/#step-11-commit","title":"Step 11: Commit","text":"<pre><code># Add to .gitignore\necho \"alloy.state.yml\" &gt;&gt; .gitignore\necho \".env\" &gt;&gt; .gitignore\n\ngit add manifest.yml 00-system.yml 10-toolchain.yml 20-environment.yml\ngit add alloy.lock.yml  # if it exists\ngit commit -m \"feat: add ARM Cortex-M development environment blueprint\"\n</code></pre>"},{"location":"blueprints/from-scratch/#complete-file-listing","title":"Complete file listing","text":"<pre><code>my-arm-env/\n\u251c\u2500\u2500 .gitignore\n\u251c\u2500\u2500 manifest.yml\n\u251c\u2500\u2500 00-system.yml\n\u251c\u2500\u2500 10-toolchain.yml\n\u2514\u2500\u2500 20-environment.yml\n</code></pre>"},{"location":"blueprints/from-scratch/#next-steps","title":"Next steps","text":"<ul> <li>Add variables.yml and env files: credentials and per-machine settings</li> <li>Extend with more layers: project-specific tools and udev rules</li> <li>Publish to Alloy Hub: share with the community</li> </ul>"},{"location":"blueprints/layers/","title":"The Layers Approach","text":""},{"location":"blueprints/layers/#why-layers-matter","title":"Why layers matter","text":"<p>A typical embedded build environment involves several independent concerns: base OS packages, a cross-compilation toolchain, SDK setup, environment variables, and project-specific tools. Without structure, these all end up in one sprawling script or a single task file that becomes impossible to maintain.</p> <p>The layers approach splits a blueprint into numbered files, each responsible for one logical layer of the environment. This is the pattern followed by all blueprints published in the alloy-catalog, and it is strongly recommended for any blueprint you write.</p>"},{"location":"blueprints/layers/#recommended-layer-structure","title":"Recommended layer structure","text":"<pre><code>my-blueprint/\n\u251c\u2500\u2500 manifest.yml\n\u251c\u2500\u2500 00-system.yml       # base OS packages\n\u251c\u2500\u2500 10-toolchain.yml    # cross-compiler / SDK download and install\n\u251c\u2500\u2500 20-environment.yml  # env vars, profile scripts, CMake config\n\u251c\u2500\u2500 30-project.yml      # project-specific tools, udev rules (optional)\n\u2514\u2500\u2500 alloy.lock.yml\n</code></pre> <p>Each file is independent, idempotent, and focused on a single concern.</p>"},{"location":"blueprints/layers/#layer-0-system-base-00-systemyml","title":"Layer 0: System base (<code>00-system.yml</code>)","text":"<p>Install the base OS packages and system-level configuration. These are the foundation everything else depends on.</p> <pre><code>- name: Update APT cache\n  action: apt_install\n  packages: []\n  always_run: true\n\n- name: Install build essentials\n  action: apt_install\n  packages:\n    - git\n    - cmake\n    - ninja-build\n    - python3\n    - python3-pip\n    - wget\n    - curl\n    - ca-certificates\n    - rsync\n    - tar\n    - xz-utils\n\n- name: Install debugging tools\n  action: apt_install\n  packages:\n    - gdb-multiarch\n    - openocd\n</code></pre> <p>Keep this layer minimal. Avoid putting toolchain-specific packages here; put them in the toolchain layer.</p>"},{"location":"blueprints/layers/#layer-1-toolchain-10-toolchainyml","title":"Layer 1: Toolchain (<code>10-toolchain.yml</code>)","text":"<p>Download and install the cross-compilation toolchain or SDK. This is usually the heaviest layer, as it downloads large archives.</p> <pre><code>- name: Install ARM GCC toolchain\n  action: unarchive_from_url\n  dest: \"{{.Vars.ARM_TOOLCHAIN_DEST}}\"\n  strip_components: 1\n  creates: \"{{.Vars.ARM_TOOLCHAIN_DEST}}/bin/arm-none-eabi-gcc\"\n  per_arch:\n    amd64:\n      url: https://developer.arm.com/-/media/Files/downloads/gnu/13.3.rel1/binrel/arm-gnu-toolchain-13.3.rel1-x86_64-arm-none-eabi.tar.xz\n      sha256: \"aaabbbccc...\"\n    arm64:\n      url: https://developer.arm.com/-/media/Files/downloads/gnu/13.3.rel1/binrel/arm-gnu-toolchain-13.3.rel1-aarch64-arm-none-eabi.tar.xz\n      sha256: \"dddeeefff...\"\n</code></pre> <p>Always include SHA256</p> <p>Every download must include a <code>sha256</code> field. The provisioner verifies the hash and fails if it does not match. This is what makes blueprints reproducible.</p> <p>If your toolchain is in the catalog, use <code>unarchive_from_ref</code> instead; it reads the URL and SHA from <code>alloy.lock.yml</code>:</p> <pre><code>- name: Install ARM GCC toolchain\n  action: unarchive_from_ref\n  ref: toolchain.arm-gnu.arm-none-eabi@stable\n  dest: \"{{.Vars.ARM_TOOLCHAIN_DEST}}\"\n  strip_components: 1\n  creates: \"{{.Vars.ARM_TOOLCHAIN_DEST}}/bin/arm-none-eabi-gcc\"\n</code></pre>"},{"location":"blueprints/layers/#layer-2-environment-20-environmentyml","title":"Layer 2: Environment (<code>20-environment.yml</code>)","text":"<p>Configure the environment so tools are available and correctly referenced. This layer writes shell profile scripts, CMake toolchain files, and pkg-config configuration.</p> <pre><code>- name: Configure PATH for ARM toolchain\n  action: write_env_file\n  file: /etc/profile.d/10-arm-toolchain.sh\n  content: |\n    export PATH=$PATH:{{.Vars.ARM_TOOLCHAIN_DEST}}/bin\n    export ARM_NONE_EABI_GCC={{.Vars.ARM_TOOLCHAIN_DEST}}/bin/arm-none-eabi-gcc\n  owner: root:root\n  mode: \"0644\"\n\n- name: Generate CMake toolchain file\n  action: cmake_toolchain\n  file: /opt/cmake/arm-none-eabi.cmake\n  target_system: Generic\n  target_processor: ARM\n  compiler_prefix: arm-none-eabi-\n  compiler_path: \"{{.Vars.ARM_TOOLCHAIN_DEST}}/bin\"\n  owner: root:root\n  mode: \"0644\"\n</code></pre>"},{"location":"blueprints/layers/#layer-3-project-tools-30-projectyml-optional","title":"Layer 3: Project tools (<code>30-project.yml</code>, optional)","text":"<p>Project-specific tools that are not part of the general toolchain: hardware debug probes, flashing utilities, udev rules for USB devices.</p> <pre><code>- name: Install J-Link udev rules\n  action: udev_rules\n  rule_file: /etc/udev/rules.d/99-jlink.rules\n  content: |\n    SUBSYSTEM==\"usb\", ATTRS{idVendor}==\"1366\", MODE=\"0666\", GROUP=\"plugdev\"\n\n- name: Install pyOCD\n  action: python_package\n  package: pyocd\n  version: \"0.36.0\"\n  creates: /usr/local/bin/pyocd\n</code></pre>"},{"location":"blueprints/layers/#benefits-of-the-layers-approach","title":"Benefits of the layers approach","text":"<p>Readability: someone reading the blueprint immediately understands what each file does from its name and position.</p> <p>Independent re-provisioning: if you update the toolchain URL, only <code>10-toolchain.yml</code> changes. The other layers are fingerprint-identical and are skipped on the next <code>alloy-host provision</code>.</p> <p>Easier review: PRs that update the toolchain touch only <code>10-toolchain.yml</code>. PRs that add a package touch only <code>00-system.yml</code>. Changes are easy to understand and approve.</p> <p>Testability in isolation: during development, you can temporarily comment out later layers to test just the system base or just the toolchain install.</p> <p>Composition: for complex environments (e.g. Yocto with cross-compilation and an MCU SDK), layers make it natural to see the dependency chain: base \u2192 toolchain \u2192 SDK \u2192 project setup.</p>"},{"location":"blueprints/layers/#layer-naming-convention","title":"Layer naming convention","text":"<p>The <code>00-</code>, <code>10-</code>, <code>20-</code> prefix is a convention. The actual execution order is controlled by <code>run_order:</code> in <code>manifest.yml</code>. Leave gaps between numbers so you can insert new layers without renumbering:</p> <pre><code>run_order:\n  - 00-system.yml\n  - 10-toolchain.yml\n  - 15-sdk.yml        # inserted later between toolchain and env\n  - 20-environment.yml\n  - 30-project.yml\n</code></pre>"},{"location":"blueprints/layers/#real-world-examples","title":"Real-world examples","text":"<p>Browse the alloy-catalog to see how community blueprints use the layers approach. Every blueprint in the catalog follows this structure.</p>"},{"location":"blueprints/publishing/","title":"Publishing to Alloy Hub","text":"<p>Alloy Hub indexes blueprints from the alloy-catalog GitHub repository. To publish a blueprint, open a pull request to alloy-catalog; automated checks run, and on merge the blueprint becomes available on Alloy Hub.</p>"},{"location":"blueprints/publishing/#prerequisites","title":"Prerequisites","text":"<p>Before publishing, make sure your blueprint:</p> <ul> <li> Provisions successfully on both <code>linux/amd64</code> and <code>linux/arm64</code> (use <code>per_arch</code> overrides)</li> <li> Includes <code>sha256:</code> on every downloaded file</li> <li> Installs tools to documented, predictable paths</li> <li> Sets up environment variables via <code>write_env_file</code> (so tools are on <code>PATH</code> after provisioning)</li> <li> Includes udev rules for common USB debuggers (if applicable)</li> <li> Has been tested by actually provisioning it into a VM or natively</li> </ul>"},{"location":"blueprints/publishing/#step-1-fork-alloy-catalog","title":"Step 1: Fork alloy-catalog","text":"<ol> <li>Go to github.com/alloy-it/alloy-catalog</li> <li>Click Fork to create your own copy</li> </ol>"},{"location":"blueprints/publishing/#step-2-clone-your-fork","title":"Step 2: Clone your fork","text":"<pre><code>git clone https://github.com/YOUR_USERNAME/alloy-catalog.git\ncd alloy-catalog\n</code></pre>"},{"location":"blueprints/publishing/#step-3-create-a-branch","title":"Step 3: Create a branch","text":"<pre><code>git checkout -b add-my-toolchain-blueprint\n</code></pre>"},{"location":"blueprints/publishing/#step-4-add-your-blueprint","title":"Step 4: Add your blueprint","text":"<p>Place your blueprint under <code>blueprints/&lt;vendor&gt;/&lt;target&gt;/</code>:</p> <pre><code>blueprints/\n\u2514\u2500\u2500 my-vendor/\n    \u2514\u2500\u2500 my-toolchain/\n        \u251c\u2500\u2500 manifest.yml\n        \u251c\u2500\u2500 00-system.yml\n        \u251c\u2500\u2500 10-toolchain.yml\n        \u251c\u2500\u2500 20-environment.yml\n        \u251c\u2500\u2500 alloy.lock.yml     # if using catalog refs\n        \u2514\u2500\u2500 README.md          # required: see below\n</code></pre>"},{"location":"blueprints/publishing/#step-5-write-readmemd","title":"Step 5: Write README.md","text":"<p>A <code>README.md</code> is required for catalog blueprints. Include:</p> <pre><code># My Vendor \u2014 My Toolchain\n\nBrief description of what this environment installs.\n\n## Included tools\n\n| Tool | Version | Installed path |\n|---|---|---|\n| my-toolchain | 1.2.3 | `/opt/my-toolchain` |\n| cmake | system | system |\n\n## Environment variables\n\nSet by this blueprint:\n\n| Variable | Value |\n|---|---|\n| `PATH` | Includes `/opt/my-toolchain/bin` |\n| `MY_TOOLCHAIN_ROOT` | `/opt/my-toolchain` |\n\n## Supported boards / targets\n\n- Board / chip family A\n- Board / chip family B\n\n## Tested on\n\n- `linux/amd64`: Ubuntu 22.04\n- `linux/arm64`: Ubuntu 22.04\n</code></pre>"},{"location":"blueprints/publishing/#step-6-commit-and-push","title":"Step 6: Commit and push","text":"<pre><code>git add blueprints/my-vendor/\ngit commit -m \"feat: add my-toolchain blueprint\"\ngit push origin add-my-toolchain-blueprint\n</code></pre>"},{"location":"blueprints/publishing/#step-7-open-a-pull-request","title":"Step 7: Open a pull request","text":"<p>Go to your fork on GitHub and click Compare &amp; pull request. Target the <code>main</code> branch of <code>alloy-it/alloy-catalog</code>.</p> <p>In the PR description, include: - What the blueprint installs and which versions - Which boards or targets it is meant for - Which platforms you have tested it on</p>"},{"location":"blueprints/publishing/#automated-checks","title":"Automated checks","text":"<p>When you open the PR, CI runs automatically:</p> <ul> <li>YAML schema validation: checks that <code>manifest.yml</code> and task files follow the blueprint schema</li> <li>SHA256 presence: verifies that every download task includes a <code>sha256</code> field</li> <li>Structure check: confirms <code>README.md</code> exists and <code>run_order</code> matches actual task files</li> </ul> <p>Fix any errors reported by CI and push new commits to the same branch; the checks re-run automatically.</p>"},{"location":"blueprints/publishing/#after-merge","title":"After merge","text":"<p>Once the PR is merged into <code>alloy-catalog/main</code>:</p> <ul> <li>The blueprint appears on Alloy Hub within minutes.</li> <li>Users can install it with <code>alloy-provisioner install &lt;your-vendor&gt;/&lt;your-blueprint&gt;</code>.</li> <li>Users with alloy-host can find it with <code>alloy-host catalog search</code>.</li> </ul>"},{"location":"blueprints/publishing/#updating-a-published-blueprint","title":"Updating a published blueprint","text":"<p>To update a blueprint (new toolchain version, added package, bug fix):</p> <ol> <li>Create a new branch in your fork (or the alloy-catalog fork if you are a maintainer).</li> <li>Edit the relevant task files.</li> <li>Bump the version in <code>manifest.yml</code>.</li> <li>Re-run <code>alloy-host resolve</code> if catalog refs changed.</li> <li>Open a new PR.</li> </ol>"},{"location":"blueprints/publishing/#sharing-within-your-team-without-publishing","title":"Sharing within your team (without publishing)","text":"<p>You do not need to publish to Alloy Hub to share a blueprint with your team. Commit the blueprint directory and lockfile to your project repo:</p> <pre><code>git add blueprint/ alloy.lock.yml\ngit commit -m \"add development environment blueprint\"\ngit push\n</code></pre> <p>Teammates clone the repo and provision:</p> <pre><code># With alloy-host\nalloy-host init dev --blueprint ./blueprint\nalloy-host up\n\n# Native Linux\nsudo alloy-provisioner --blueprint-dir ./blueprint\n</code></pre> <p>The lockfile ensures everyone uses identical tool versions.</p>"},{"location":"blueprints/publishing/#next-steps","title":"Next steps","text":"<ul> <li>Blueprint Structure: ensure your blueprint follows the expected layout</li> <li>The Layers Approach: required for catalog blueprints</li> <li>Browse existing blueprints: see how published blueprints are structured</li> </ul>"},{"location":"blueprints/structure/","title":"Blueprint Structure","text":"<p>A blueprint is a directory containing a manifest file, one or more task files, and an optional lockfile.</p>"},{"location":"blueprints/structure/#directory-layout","title":"Directory layout","text":"<pre><code>my-blueprint/\n\u251c\u2500\u2500 manifest.yml        # required: metadata, variables, task order\n\u251c\u2500\u2500 variables.yml       # optional: base variable definitions\n\u251c\u2500\u2500 alloy.lock.yml      # optional: resolved toolchain URLs and checksums (commit this)\n\u251c\u2500\u2500 00-system.yml       # task file 1\n\u251c\u2500\u2500 10-toolchain.yml    # task file 2\n\u251c\u2500\u2500 20-environment.yml  # task file 3\n\u2514\u2500\u2500 alloy.state.yml     # auto-generated by provisioner \u2014 DO NOT commit\n</code></pre> <p>The numbering convention for task files (<code>00-</code>, <code>10-</code>, <code>20-</code>) is a recommendation, not a requirement. The explicit <code>run_order:</code> in <code>manifest.yml</code> controls execution order regardless of filename.</p>"},{"location":"blueprints/structure/#manifestyml","title":"manifest.yml","text":"<p>The manifest is the entry point. The provisioner reads it first.</p> <pre><code>name: ARM Cortex-M Development Environment # required\nversion: 1.0.0 # required\ndescription: ARM GCC toolchain for Cortex-M embedded development.\n\nvariables: # optional\n  ARM_TOOLCHAIN_DEST: /opt/arm-none-eabi\n  DEV_USERNAME: vagrant\n\ntoolchains: # optional: catalog references\n  - ref: toolchain.arm-gnu.arm-none-eabi@stable\n    alias: ARM_TOOLCHAIN # injects ARM_TOOLCHAIN_URL and ARM_TOOLCHAIN_SHA\n\nrequired_env: # optional: validate env vars before provisioning\n  - name: GITLAB_TOKEN\n    description: Token for private artifact registry\n\nsupported_hosts: # optional: document supported platforms\n  - linux/amd64\n  - linux/arm64\n\nrun_order: # required: explicit execution order\n  - 00-system.yml\n  - 10-toolchain.yml\n  - 20-environment.yml\n</code></pre>"},{"location":"blueprints/structure/#manifest-fields","title":"Manifest fields","text":"Field Required Description <code>name</code> Yes Human-readable environment name <code>version</code> Yes Semantic version (e.g. <code>1.2.0</code>) <code>description</code> No Short description <code>variables</code> No Key-value pairs; available as <code>{{.Vars.NAME}}</code> in task files <code>toolchains</code> No Catalog refs with optional aliases <code>required_env</code> No Env vars that must be set before provisioning <code>supported_hosts</code> No Documentation of supported <code>os/arch</code> platforms <code>run_order</code> Yes Ordered list of task files to execute"},{"location":"blueprints/structure/#task-files","title":"Task files","text":"<p>Task files are flat YAML lists. Each item is a task with a name, an action, and action-specific fields.</p> <pre><code>- name: Install build tools\n  action: apt_install\n  packages:\n    - git\n    - cmake\n    - ninja-build\n    - python3\n\n- name: Download ARM GCC toolchain\n  action: unarchive_from_url\n  dest: \"{{.Vars.ARM_TOOLCHAIN_DEST}}\"\n  creates: \"{{.Vars.ARM_TOOLCHAIN_DEST}}/bin/arm-none-eabi-gcc\"\n  per_arch:\n    amd64:\n      url: https://example.com/arm-gcc-13-x86_64.tar.xz\n      sha256: \"aaa111bbb222...\"\n    arm64:\n      url: https://example.com/arm-gcc-13-aarch64.tar.xz\n      sha256: \"ccc333ddd444...\"\n\n- name: Configure PATH\n  action: write_env_file\n  file: /etc/profile.d/10-arm-toolchain.sh\n  content: |\n    export PATH=$PATH:{{.Vars.ARM_TOOLCHAIN_DEST}}/bin\n  owner: root:root\n  mode: \"0644\"\n</code></pre>"},{"location":"blueprints/structure/#common-task-fields","title":"Common task fields","text":"Field Description <code>name</code> Required. Human-readable task identifier. <code>action</code> Required. The operation to perform (see below). <code>creates</code> Path to check; if it exists, skip the task. The fastest idempotency mechanism. <code>always_run</code> If <code>true</code>, bypass all idempotency checks and always execute. <code>skip_if</code> Shell expression; if it exits <code>0</code>, skip the task entirely. Takes precedence over <code>always_run</code>. <code>run_as</code> Run the command as this user (e.g. <code>vagrant</code>). Defaults to root. <code>owner</code> Set file ownership after execution (e.g. <code>vagrant:vagrant</code>). <code>mode</code> Set file permissions after execution (e.g. <code>0755</code> or <code>u+x</code>). <code>per_arch</code> Architecture-specific overrides for <code>url</code>, <code>sha256</code>, <code>command</code>, or <code>source</code>."},{"location":"blueprints/structure/#architecture-overrides-per_arch","title":"Architecture overrides (<code>per_arch</code>)","text":"<p>Use <code>per_arch</code> to handle <code>amd64</code> and <code>arm64</code> differences without conditional logic:</p> <pre><code>- name: Install toolchain\n  action: unarchive_from_url\n  dest: /opt/my-toolchain\n  creates: /opt/my-toolchain/bin/my-gcc\n  per_arch:\n    amd64:\n      url: https://example.com/toolchain-x86_64.tar.gz\n      sha256: \"abc123...\"\n    arm64:\n      url: https://example.com/toolchain-aarch64.tar.gz\n      sha256: \"def456...\"\n</code></pre> <p>The provisioner detects the host architecture and merges the matching block before executing the task.</p>"},{"location":"blueprints/structure/#common-actions","title":"Common actions","text":"Action What it does <code>apt_install</code> Install packages via apt-get (only installs missing packages) <code>run_command</code> Execute a shell command <code>get_file</code> Download a file with SHA256 verification <code>unarchive_from_url</code> Download and extract an archive <code>unarchive_from_ref</code> Extract a toolchain resolved from the lockfile <code>write_env_file</code> Write a file (env script, config, toolchain file) <code>udev_rules</code> Install a udev rules file and reload udev <code>python_package</code> Install a Python package via pip <code>rust_package</code> Install a Rust package via cargo <p>Full action reference: Blueprint Syntax</p>"},{"location":"blueprints/structure/#alloylockyml","title":"alloy.lock.yml","text":"<p>The lockfile pins catalog toolchain references to exact download URLs and SHA256 checksums. Generate it with:</p> <pre><code>alloy-host resolve        # if using alloy-host\n</code></pre> <pre><code>resolved_at: \"2026-01-15T10:00:00Z\"\nhost: linux/amd64\ntoolchains:\n  toolchain.arm-gnu.arm-none-eabi@stable:\n    url: https://api.alloy-it.io/toolchains/arm-gnu/arm-none-eabi/stable/arm-gnu-13.3.tar.xz\n    sha256: \"abc123def456...\"\n    method: archive\n</code></pre> <p>Always commit the lockfile. It ensures every developer, CI runner, and Docker build uses byte-for-byte identical toolchains.</p>"},{"location":"blueprints/structure/#alloystateyml","title":"alloy.state.yml","text":"<p>The provisioner creates <code>alloy.state.yml</code> after each run. It records a fingerprint of every completed task so re-runs can skip unchanged work.</p> <p>Do not commit <code>alloy.state.yml</code>. Add it to <code>.gitignore</code>:</p> <pre><code>alloy.state.yml\n.env\n</code></pre>"},{"location":"blueprints/structure/#next-steps","title":"Next steps","text":"<ul> <li>Variables &amp; Configuration: manifest vars, variables.yml, env files</li> <li>The Layers Approach: recommended file organization</li> <li>Blueprint Syntax Reference: all actions and fields</li> </ul>"},{"location":"blueprints/variables/","title":"Variables &amp; Configuration","text":"<p>Blueprints support three complementary mechanisms for injecting values into tasks: manifest variables, a separate <code>variables.yml</code> file, and environment variables loaded from a <code>.env</code> file. Each serves a different purpose.</p>"},{"location":"blueprints/variables/#1-manifest-variables","title":"1. Manifest variables","text":"<p>Define static values in <code>manifest.yml</code> under <code>variables:</code>. These are available in all task file fields using Go template syntax:</p> <pre><code># manifest.yml\nvariables:\n  ARM_TOOLCHAIN_DEST: /opt/arm-none-eabi\n  DEV_USERNAME: vagrant\n  TOOLCHAIN_VERSION: \"13.3.rel1\"\n</code></pre> <p>Reference them in task files with <code>{{.Vars.NAME}}</code>:</p> <pre><code># 10-toolchain.yml\n- name: Install ARM GCC toolchain\n  action: unarchive_from_url\n  dest: \"{{.Vars.ARM_TOOLCHAIN_DEST}}\"\n  creates: \"{{.Vars.ARM_TOOLCHAIN_DEST}}/bin/arm-none-eabi-gcc\"\n\n- name: Configure PATH\n  action: write_env_file\n  file: /etc/profile.d/10-arm-toolchain.sh\n  content: |\n    export PATH=$PATH:{{.Vars.ARM_TOOLCHAIN_DEST}}/bin\n    export ARM_TOOLCHAIN_VERSION={{.Vars.TOOLCHAIN_VERSION}}\n</code></pre> <p>Manifest variables are good for values that are the same across all environments: installation paths, tool names, version strings.</p>"},{"location":"blueprints/variables/#2-variablesyml","title":"2. variables.yml","text":"<p>For larger blueprints or when you want to separate variable definitions from blueprint metadata, create a <code>variables.yml</code> file:</p> <pre><code># variables.yml\nARM_TOOLCHAIN_DEST: /opt/arm-none-eabi\nDEV_USERNAME: vagrant\nTOOLCHAIN_VERSION: \"13.3.rel1\"\nSDK_DEST: /opt/sdk\n</code></pre> <p><code>variables.yml</code> is merged with manifest variables before task execution. Manifest <code>variables:</code> takes precedence over <code>variables.yml</code> , so you can use <code>variables.yml</code> as a base and override specific values in <code>manifest.yml</code>.</p> <p>This is useful when:</p> <ul> <li>Multiple blueprints share common variable definitions.</li> <li>You want to keep <code>manifest.yml</code> focused on metadata and structure.</li> <li>The set of variables is large and would clutter the manifest.</li> </ul>"},{"location":"blueprints/variables/#3-environment-file-env","title":"3. Environment file (<code>.env</code>)","text":"<p>Use a <code>.env</code> file for values that change per machine or contain credentials: registry tokens, usernames, private artifact URLs. These should never be committed.</p> <p>Create a <code>.env</code> file in your blueprint directory:</p> <pre><code># .env \u2014 never commit this file\nGITLAB_TOKEN=glpat-xxxxxxxxxxxx\nSDK_DESTINATION=/opt/my-sdk\nDEV_USERNAME=ubuntu\nALLOY_REGISTRY_USERNAME=myuser\nALLOY_REGISTRY_PASSWORD=my-token\n</code></pre> <p>Source it before running the provisioner:</p> <pre><code>set -a &amp;&amp; source .env &amp;&amp; set +a\nsudo -E alloy-provisioner --blueprint-dir /path/to/blueprint\n</code></pre> <p>Or use the <code>-env-file</code> flag:</p> <pre><code>sudo alloy-provisioner --blueprint-dir /path/to/blueprint -env-file /path/to/.env\n</code></pre> <p>When using alloy-host, pass the env file at init time or place it in the blueprint directory; alloy-host picks it up when running the provisioner inside the guest.</p> <p>Never commit <code>.env</code> files</p> <p>Add <code>.env</code> to <code>.gitignore</code>. For CI/CD, use pipeline secret management (GitHub Secrets, GitLab CI Variables, etc.) and inject them as environment variables.</p>"},{"location":"blueprints/variables/#4-required-environment-variables","title":"4. Required environment variables","text":"<p>Use <code>required_env:</code> in <code>manifest.yml</code> to declare which environment variables must be set before provisioning runs. If any are missing, the provisioner exits with a clear error listing all missing variables and instructions for how to set them.</p> <pre><code># manifest.yml\nrequired_env:\n  - name: GITLAB_TOKEN\n    description: Personal access token for the private artifact registry\n  - name: SDK_DESTINATION\n    description: Where to install the proprietary SDK (e.g. /opt/vendor-sdk)\n</code></pre> <p>When a required variable is missing:</p> <pre><code>Error: missing required environment variables:\n  GITLAB_TOKEN \u2014 Personal access token for the private artifact registry\n  SDK_DESTINATION \u2014 Where to install the proprietary SDK\n\nSet them in a .env file and run:\n  set -a &amp;&amp; source .env &amp;&amp; set +a &amp;&amp; alloy-provisioner --blueprint-dir .\n</code></pre>"},{"location":"blueprints/variables/#5-toolchain-alias-injection","title":"5. Toolchain alias injection","text":"<p>When a <code>toolchains:</code> entry in <code>manifest.yml</code> has an <code>alias</code>, the provisioner automatically injects two variables after resolving the lockfile:</p> <pre><code># manifest.yml\ntoolchains:\n  - ref: toolchain.arm-gnu.arm-none-eabi@stable\n    alias: ARM_TOOLCHAIN\n</code></pre> <p>This makes <code>{{.Vars.ARM_TOOLCHAIN_URL}}</code> and <code>{{.Vars.ARM_TOOLCHAIN_SHA}}</code> , pointing to the exact URL and checksum from <code>alloy.lock.yml</code>.</p> <p>Use them with <code>unarchive_from_ref</code> (recommended; uses the lockfile automatically):</p> <pre><code>- name: Install ARM GCC via catalog ref\n  action: unarchive_from_ref\n  ref: toolchain.arm-gnu.arm-none-eabi@stable\n  dest: \"{{.Vars.ARM_TOOLCHAIN_DEST}}\"\n  strip_components: 1\n  creates: \"{{.Vars.ARM_TOOLCHAIN_DEST}}/bin/arm-none-eabi-gcc\"\n</code></pre> <p>Or with <code>unarchive_from_url</code> if you need the URL and SHA explicitly:</p> <pre><code>- name: Install ARM GCC\n  action: unarchive_from_url\n  url: \"{{.Vars.ARM_TOOLCHAIN_URL}}\"\n  sha256: \"{{.Vars.ARM_TOOLCHAIN_SHA}}\"\n  dest: \"{{.Vars.ARM_TOOLCHAIN_DEST}}\"\n</code></pre>"},{"location":"blueprints/variables/#variable-resolution-order","title":"Variable resolution order","text":"<p>Variables are resolved in this order (later entries win on conflict):</p> <ol> <li><code>variables.yml</code>: base definitions</li> <li><code>manifest.yml</code> <code>variables:</code>: overrides <code>variables.yml</code></li> <li>Process environment (including <code>.env</code>): overrides manifest variables</li> <li>Toolchain alias injection: appended after lockfile resolution</li> </ol>"},{"location":"blueprints/variables/#security-note","title":"Security note","text":"<p>The <code>run_command.command</code> field is not expanded for environment variables. : it prevents command injection when commands come from user-controlled input. All other task fields that accept strings are safely expanded.</p>"},{"location":"blueprints/variables/#summary","title":"Summary","text":"Mechanism Use for Syntax Commit? <code>manifest.yml variables:</code> Static values, paths, versions <code>{{.Vars.NAME}}</code> Yes <code>variables.yml</code> Shared base variables <code>{{.Vars.NAME}}</code> Yes <code>.env</code> file Credentials, per-machine settings <code>$NAME</code> (env var) No <code>required_env:</code> Declare mandatory env vars Declaration only Yes Toolchain alias Auto-injected URL + SHA from lockfile <code>{{.Vars.ALIAS_URL}}</code> n/a"},{"location":"catalog/","title":"Available Toolchains","text":""},{"location":"catalog/#alloy-hub-and-the-catalog","title":"Alloy Hub and the catalog","text":"<p>Alloy Hub is the web-facing registry for community-maintained blueprints and toolchains. Browse and install environments directly from there, or use the command line.</p> <p>The catalog is the metadata store that backs Alloy Hub. It does not store binaries; it stores toolchain IDs, versions, download URLs, and SHA256 checksums per host platform (<code>linux/amd64</code>, <code>linux/arm64</code>). The catalog lives in the alloy-catalog GitHub repository and is synced locally by <code>alloy-host catalog update</code>.</p>"},{"location":"catalog/#how-blueprints-use-the-catalog","title":"How blueprints use the catalog","text":"<p>Blueprints declare which tools they need using refs (e.g. <code>toolchain.arm-gnu.arm-none-eabi@stable</code>). The catalog defines what those refs mean: the actual download URL and SHA256 for each supported architecture.</p> <p>When you run <code>alloy-host resolve</code>, alloy-host looks up each ref in the local catalog and writes <code>alloy.lock.yml</code> with pinned URLs and checksums. The provisioner uses the lockfile to download exact artifacts, reproducibly.</p> <p>This means: blueprints say \"I need this tool at this version\"; the catalog says \"here is where to get it and how to verify it\"; the lockfile records the resolved result.</p>"},{"location":"catalog/#why-it-scales","title":"Why it scales","text":"<p>One catalog entry is maintained once and consumed by many blueprints and teams. You do not copy URLs and checksums into every project; you reference a catalog ID. When a new toolchain version is added, any blueprint using <code>@stable</code> can pick it up by re-running <code>alloy-host resolve</code>.</p>"},{"location":"catalog/#workflow-using-the-catalog-in-a-blueprint","title":"Workflow: using the catalog in a blueprint","text":"<pre><code>flowchart LR\n  Update[catalog update] --&gt; Search[search / info]\n  Search --&gt; Ref[add ref to blueprint]\n  Ref --&gt; Resolve[alloy-host resolve]\n  Resolve --&gt; Lockfile[commit lockfile]</code></pre> Step Command 1. Update catalog <code>alloy-host catalog update</code> 2. Find an entry <code>alloy-host catalog search &lt;term&gt;</code> then <code>alloy-host catalog info &lt;id&gt;</code> 3. Reference in blueprint Add <code>toolchains:</code> in <code>manifest.yml</code> or use <code>unarchive_from_ref</code> in task file 4. Resolve <code>alloy-host resolve</code> \u2192 writes <code>alloy.lock.yml</code> 5. Commit lockfile Commit <code>alloy.lock.yml</code> for reproducibility"},{"location":"catalog/#keeping-the-catalog-up-to-date","title":"Keeping the catalog up to date","text":"<pre><code>alloy-host catalog update\n</code></pre> <p>This clones or pulls the latest catalog into <code>~/.alloy-it/catalog/</code>. Run this before searching or resolving.</p>"},{"location":"catalog/#browsing-the-catalog","title":"Browsing the catalog","text":"<pre><code>alloy-host catalog search arm\nalloy-host catalog search nordic\nalloy-host catalog search golang\n</code></pre> <p>To see all versions of a specific entry:</p> <pre><code>alloy-host catalog info toolchain.arm-gnu.arm-none-eabi\nalloy-host catalog info sdk.golang.go\n</code></pre> <p>Or browse directly on Alloy Hub.</p>"},{"location":"catalog/#reference-format","title":"Reference format","text":"<p>Catalog entries are referenced using dotted IDs with an optional version or alias:</p> <pre><code>toolchain.arm-gnu.arm-none-eabi@stable\ntoolchain.arm-gnu.arm-none-eabi@13.3.rel1\nsdk.golang.go@stable\ntool.nordic.nrf-command-line-tools@stable\n</code></pre> <p>Use <code>@stable</code> to always resolve to the latest stable release, or pin to a specific version number. The resolved version is recorded in <code>alloy.lock.yml</code> when you run <code>alloy-host resolve</code>.</p>"},{"location":"catalog/#available-entries","title":"Available entries","text":"<p>Note</p> <p>The catalog is updated independently of this documentation. Run <code>alloy-host catalog search</code> or browse Alloy Hub for the current list. The entries below reflect what is available at the time of writing.</p>"},{"location":"catalog/#arm-toolchains","title":"ARM toolchains","text":"Catalog ID Description <code>toolchain.arm-gnu.arm-none-eabi</code> ARM GNU Toolchain (bare metal, Cortex-M/R) <code>toolchain.arm-gnu.arm-none-linux-gnueabihf</code> ARM GNU Toolchain (Linux, 32-bit) <code>toolchain.arm-gnu.aarch64-none-linux-gnu</code> ARM GNU Toolchain (Linux, 64-bit)"},{"location":"catalog/#sdks-and-runtimes","title":"SDKs and runtimes","text":"Catalog ID Description <code>sdk.golang.go</code> Go programming language"},{"location":"catalog/#vendor-tools","title":"Vendor tools","text":"Catalog ID Description <code>tool.nordic.nrf-command-line-tools</code> Nordic nRF Command Line Tools"},{"location":"catalog/#using-a-catalog-entry-in-a-blueprint","title":"Using a catalog entry in a blueprint","text":"<p>Declare catalog references in <code>manifest.yml</code>:</p> <pre><code>toolchains:\n  - ref: toolchain.arm-gnu.arm-none-eabi@stable\n    alias: ARM_GCC # injects ARM_GCC_URL and ARM_GCC_SHA as variables\n</code></pre> <p>Or use <code>unarchive_from_ref</code> directly in a task file:</p> <pre><code>- name: Install ARM GNU toolchain\n  action: unarchive_from_ref\n  ref: toolchain.arm-gnu.arm-none-eabi@stable\n  dest: /opt/arm-none-eabi\n  strip_components: 1\n  creates: /opt/arm-none-eabi/bin/arm-none-eabi-gcc\n</code></pre> <p>Then generate the lockfile:</p> <pre><code>alloy-host resolve\n</code></pre>"},{"location":"catalog/#contributing-a-toolchain","title":"Contributing a toolchain","text":"<p>To add a new toolchain to the catalog and make it available on Alloy Hub, see Publishing to Alloy Hub.</p>"},{"location":"contributing/","title":"Contributing","text":"<p>Alloy is open source and contributions are welcome. This page covers how to contribute blueprints, report issues, and contribute to the core tools.</p>"},{"location":"contributing/#contributing-a-blueprint-to-alloy-hub","title":"Contributing a blueprint to Alloy Hub","text":"<p>The fastest way to help the community is to contribute a blueprint for a toolchain you're already using.</p> <p>See the full guide: Publishing to Alloy Hub</p>"},{"location":"contributing/#what-makes-a-good-catalog-blueprint","title":"What makes a good catalog blueprint","text":"<ul> <li>Works on both <code>amd64</code> and <code>arm64</code> hosts (use <code>per_arch</code> overrides for architecture-specific downloads)</li> <li>Includes <code>sha256:</code> checksums on all downloads</li> <li>Installs tools to predictable, documented paths</li> <li>Sets up environment variables via <code>write_env_file</code> so tools are immediately usable after provisioning</li> <li>Includes udev rules for common USB debuggers (if applicable)</li> <li>Has been tested by actually provisioning a VM or machine with it</li> <li>Follows the layers approach: <code>00-system.yml</code>, <code>10-toolchain.yml</code>, <code>20-environment.yml</code></li> </ul>"},{"location":"contributing/#quick-steps","title":"Quick steps","text":"<ol> <li>Fork alloy-catalog on GitHub</li> <li>Create a branch and add your blueprint under <code>blueprints/&lt;vendor&gt;/&lt;target&gt;/</code></li> <li>Include a <code>README.md</code> documenting installed tools, paths, env vars, and tested platforms</li> <li>Open a pull request; automated checks validate schema, SHA256 presence, and structure</li> <li>On merge, the blueprint appears on Alloy Hub</li> </ol>"},{"location":"contributing/#reporting-issues","title":"Reporting issues","text":"<ul> <li>Alloy Host (CLI): github.com/alloy-it/alloy-host/issues</li> <li>Alloy Provisioner: github.com/alloy-it/alloy-provisioner/issues</li> <li>Catalog blueprints: github.com/alloy-it/alloy-catalog/issues</li> <li>Documentation: github.com/alloy-it/alloy-docs/issues</li> </ul> <p>When reporting a bug, please include:</p> <ul> <li>Your host OS and architecture</li> <li><code>alloy-host check-health</code> output (if using alloy-host)</li> <li>The command you ran and the full output</li> <li>Your blueprint (if relevant)</li> </ul>"},{"location":"contributing/#contributing-to-alloy-host","title":"Contributing to Alloy Host","text":"<p>Alloy Host is written in Go. To set up a development environment:</p> <pre><code>git clone https://github.com/alloy-it/alloy-host\ncd alloy-host\nmake build\n./alloy-host check-health\n</code></pre> <p>Run tests and lint:</p> <pre><code>make test\nmake lint\n</code></pre> <p>Open a pull request with a description of what the change does and why.</p>"},{"location":"contributing/#contributing-to-alloy-provisioner","title":"Contributing to Alloy Provisioner","text":"<p>Alloy Provisioner is also written in Go and uses a ports-and-adapters architecture. Each task action is an independent adapter implementing a single interface, making it straightforward to add new actions.</p> <pre><code>git clone https://github.com/alloy-it/alloy-provisioner\ncd alloy-provisioner\nmake build\nmake test\n</code></pre>"},{"location":"contributing/#improving-the-documentation","title":"Improving the documentation","text":"<p>Documentation is in the alloy-docs repository. To run it locally:</p> <pre><code>cd alloy-docs\nsource .venv/bin/activate    # activate the Python venv\nmkdocs serve\n</code></pre> <p>Open http://localhost:8000 to preview your changes. Submit a pull request with corrections, clarifications, or new content.</p>"},{"location":"get-started/","title":"Get Started","text":"<p>alloy-provisioner is the engine that installs build environments from blueprints. It runs inside any Linux system. Choose the path that matches your situation:</p> <ul> <li> <p> On Linux (Native)</p> <p>You have a Linux machine (bare metal, a test rack, an existing VM managed by your own tools like Vagrant, VirtualBox, or Proxmox, or a WSL2 distro). Install alloy-provisioner and run blueprints directly.</p> <p>Best for: CI servers, test racks, shared build machines, manually provisioning your own VMs.</p> <p>Install natively \u2192</p> </li> <li> <p> With Alloy Host</p> <p>You are on a developer workstation (Windows, macOS, or Linux) and want isolated environments that don't touch your host machine. alloy-host manages VM and Docker lifecycles for you.</p> <p>Best for: local development, quickly testing a blueprint before publishing, switching between project environments.</p> <p>Use alloy-host \u2192</p> </li> <li> <p> In a Docker Image</p> <p>You want to replace a complex, hard-to-maintain Dockerfile with a declarative blueprint. alloy-provisioner runs inside the Docker build and produces a reproducible image using the same definition used by developers and CI.</p> <p>Best for: CI base images, replacing multi-stage Dockerfiles and chains of dependent images.</p> <p>Build Docker images \u2192</p> </li> </ul>"},{"location":"get-started/#which-path-should-i-choose","title":"Which path should I choose?","text":"Situation Recommended path CI server or test rack (bare-metal Linux) Native Existing VM I manage myself Native: run alloy-provisioner inside the VM Developer workstation (want isolation) alloy-host Testing a blueprint before publishing alloy-host Docker CI image Docker Multiple devs, same environment Any path, all using the same blueprint"},{"location":"get-started/#before-you-start","title":"Before you start","text":"<p>All paths require a Linux environment with:</p> <ul> <li>curl and wget: for downloading alloy-provisioner and toolchains</li> <li>apt: alloy-provisioner targets Debian/Ubuntu-based systems</li> <li>sudo or root access: provisioner tasks install system packages and tools</li> </ul> <p>If you are using alloy-host or Docker, these requirements apply to the guest (the VM or container), not your host machine.</p>"},{"location":"hardware/usb-passthrough/","title":"USB &amp; Target Boards","text":"<p>Alloy supports passing USB devices (debuggers, development boards, programmers) from your host machine into the VM. You can flash firmware and run a debug session without leaving your development environment.</p>"},{"location":"hardware/usb-passthrough/#prerequisites","title":"Prerequisites","text":"VirtualBox (macOS / Linux)WSL2 (Windows) <p>USB passthrough is built into VirtualBox. No additional setup needed.</p> <p>Make sure you're in the <code>vboxusers</code> group on Linux: <pre><code>sudo usermod -aG vboxusers $USER\n# log out and back in for the change to take effect\n</code></pre></p> <p>WSL2 requires usbipd-win to share USB devices with WSL:</p> <pre><code>winget install usbipd\n</code></pre> <p>After installing, reboot your machine.</p>"},{"location":"hardware/usb-passthrough/#list-connected-usb-devices","title":"List connected USB devices","text":"<p>Connect your board or debugger, then:</p> <pre><code>alloy-host usb list\n</code></pre> <pre><code>ID    DEVICE                              STATUS\n1     J-Link Ultra+ (SEGGER)              available\n2     STM32 STLink                        available\n3     CP2102 USB to UART Bridge           available\n</code></pre>"},{"location":"hardware/usb-passthrough/#attach-a-device-to-the-vm","title":"Attach a device to the VM","text":"<pre><code>alloy-host usb attach arm-dev --id 1\n</code></pre> <p>The device is now accessible inside the VM. Verify from inside the VM:</p> <pre><code>alloy-host ssh arm-dev -- lsusb\n</code></pre> <pre><code>Bus 002 Device 003: ID 1366:0105 SEGGER J-Link Ultra+\n</code></pre>"},{"location":"hardware/usb-passthrough/#flash-firmware","title":"Flash firmware","text":"<p>Once the device is attached, use your normal flash tool from inside the VM:</p> <pre><code>alloy-host ssh arm-dev\n# inside the VM:\nJLinkExe -device nRF9160 -if SWD -speed 4000 -commandfile flash.jlink\n# or with OpenOCD:\nopenocd -f interface/jlink.cfg -f target/nrf9160.cfg -c \"program firmware.hex verify reset exit\"\n# or with pyocd:\npyocd flash --target nrf9160 firmware.bin\n</code></pre> <p>The device behaves exactly as it would if you were running these tools directly on your host machine.</p>"},{"location":"hardware/usb-passthrough/#detach-a-device","title":"Detach a device","text":"<pre><code>alloy-host usb detach arm-dev --id 1\n</code></pre> <p>The device is released back to the host and is available to other applications.</p>"},{"location":"hardware/usb-passthrough/#check-attachment-status","title":"Check attachment status","text":"<pre><code>alloy-host usb status arm-dev\n</code></pre> <pre><code>ID    DEVICE                              STATUS\n1     J-Link Ultra+ (SEGGER)              attached \u2192 arm-dev\n2     STM32 STLink                        available\n</code></pre>"},{"location":"hardware/usb-passthrough/#troubleshooting","title":"Troubleshooting","text":"<p>Device shows as \"available\" but <code>lsusb</code> doesn't show it inside the VM</p> <p>Try detaching and re-attaching: <pre><code>alloy-host usb detach arm-dev --id 1\nalloy-host usb attach arm-dev --id 1\n</code></pre></p> <p>On Linux: permission denied accessing the device</p> <p>Your blueprint should include udev rules for your debugger. Add a task to install the appropriate rules file:</p> <pre><code>  - name: Install J-Link udev rules\n    get_file:\n      url: \"https://raw.githubusercontent.com/your-org/configs/main/99-jlink.rules\"\n      sha256: \"...\"\n      dest: /etc/udev/rules.d/99-jlink.rules\n    run_command:\n      command: udevadm control --reload-rules &amp;&amp; udevadm trigger\n</code></pre> <p>On Windows (WSL2): device not showing up</p> <p>Run this in PowerShell (as administrator): <pre><code>usbipd list\nusbipd bind --busid &lt;BUSID&gt;\nusbipd attach --wsl --busid &lt;BUSID&gt;\n</code></pre></p> <p>Then run <code>alloy-host usb attach</code> from WSL.</p>"},{"location":"installing/","title":"Installing Environments","text":""},{"location":"installing/#the-core-idea","title":"The core idea","text":"<p>alloy-provisioner is a single binary that reads a blueprint and installs a complete build environment into any Linux system. The same blueprint, the same result, regardless of whether the target is a bare-metal server, a VM created by any tool, or a Docker container.</p> <pre><code>flowchart LR\n    HUB[\"Alloy Hub\\nalloy-it.io\"]\n    BP[\"Local Blueprint\\ndirectory\"]\n    PROV[\"alloy-provisioner\"]\n\n    HUB --&gt;|\"alloy-provisioner install\"| PROV\n    BP --&gt;|\"--blueprint-dir\"| PROV\n\n    PROV --&gt; ENV1[\"Linux machine\\n(bare metal / test rack)\"]\n    PROV --&gt; ENV2[\"VM\\n(any hypervisor)\"]\n    PROV --&gt; ENV3[\"Docker image\"]</code></pre>"},{"location":"installing/#what-alloy-provisioner-does","title":"What alloy-provisioner does","text":"<p>Given a blueprint directory, alloy-provisioner:</p> <ol> <li>Reads <code>manifest.yml</code> to understand variables, toolchain refs, and task order.</li> <li>Executes each task file in the declared order (install packages, download toolchains, write env files, run commands).</li> <li>Tracks what it has done in <code>alloy.state.yml</code> so re-runs skip completed tasks.</li> <li>Verifies every downloaded file against its SHA256 checksum.</li> </ol> <p>All three installation paths below use the same provisioner. There is no difference in the resulting environment.</p>"},{"location":"installing/#blueprints-from-alloy-hub","title":"Blueprints from Alloy Hub","text":"<p>Alloy Hub hosts community-maintained blueprints. You can pull and install them in one step:</p> <pre><code>alloy-provisioner install community/arm-none-eabi\n</code></pre> <p>Or search the catalog locally:</p> <pre><code>alloy-host catalog update\nalloy-host catalog search arm\n</code></pre>"},{"location":"installing/#choose-your-path","title":"Choose your path","text":"Path When to use On Linux (Native) Bare metal, test rack, existing VM, WSL2: any Linux system you already manage With Alloy Host Developer workstation: isolate environments without touching the host In a Docker Image Replace complex Dockerfiles with a declarative blueprint; CI base images"},{"location":"installing/in-docker/","title":"In a Docker Image","text":"<p>Use alloy-provisioner inside a Docker build to create reproducible CI base images from a declarative blueprint, the same one your developers use on their workstations.</p>"},{"location":"installing/in-docker/#the-problem-with-complex-dockerfiles","title":"The problem with complex Dockerfiles","text":"<p>Large embedded projects often end up with hundreds of imperative <code>RUN</code> lines in a Dockerfile, chains of <code>FROM</code> dependencies between images, and ad-hoc install scripts duplicated across repos. When the toolchain changes, multiple Dockerfiles need updating. When a junior dev needs to understand the environment, they have to parse imperative shell code.</p> <p>Blueprints solve this:</p> <ul> <li>Declarative: describe what you want, not how to do it.</li> <li>Reproducible: the same blueprint gives the same result every time, with SHA256-verified downloads.</li> <li>Shared: the same blueprint works in Docker, in a local VM, and on a CI server. No drift between developer environments and CI.</li> <li>Maintainable: layers approach splits the environment into readable, independently updatable files.</li> </ul>"},{"location":"installing/in-docker/#how-it-works","title":"How it works","text":"<p>Add alloy-provisioner to your Docker build, copy the blueprint in, run it, then remove the blueprint files to keep the final image lean:</p> <pre><code>FROM ubuntu:24.04\n\n# Non-interactive APT and safer shell\nENV DEBIAN_FRONTEND=noninteractive\nSHELL [\"/bin/bash\", \"-eo\", \"pipefail\", \"-c\"]\n\n# Install minimal dependencies for alloy-provisioner\nRUN apt-get update -qq \\\n    &amp;&amp; apt-get install --no-install-recommends -y \\\n        wget \\\n        ca-certificates \\\n        rsync \\\n        tar \\\n    &amp;&amp; rm -rf /var/lib/apt/lists/*\n\n# Download and install alloy-provisioner\nARG PROVISIONER_VERSION=latest\nRUN ARCH=$(dpkg --print-architecture) \\\n    &amp;&amp; wget -q -O /tmp/alloy-provisioner.deb \\\n        \"https://github.com/alloy-it/alloy-provisioner-releases/releases/download/${PROVISIONER_VERSION}/alloy-provisioner_${PROVISIONER_VERSION}_linux_${ARCH}.deb\" \\\n    &amp;&amp; apt-get update -qq \\\n    &amp;&amp; apt-get install -y /tmp/alloy-provisioner.deb \\\n    &amp;&amp; rm -f /tmp/alloy-provisioner.deb \\\n    &amp;&amp; rm -rf /var/lib/apt/lists/*\n\n# Copy the blueprint and run the provisioner\nCOPY blueprint/ /tmp/blueprint/\nRUN alloy-provisioner --blueprint-dir /tmp/blueprint \\\n    &amp;&amp; rm -rf /tmp/blueprint\n</code></pre> <p>Place this Dockerfile next to your blueprint directory:</p> <pre><code>my-ci-image/\n\u251c\u2500\u2500 Dockerfile\n\u2514\u2500\u2500 blueprint/\n    \u251c\u2500\u2500 manifest.yml\n    \u251c\u2500\u2500 variables.yml       # optional\n    \u251c\u2500\u2500 00-system.yml\n    \u251c\u2500\u2500 10-toolchain.yml\n    \u251c\u2500\u2500 20-environment.yml\n    \u2514\u2500\u2500 alloy.lock.yml\n</code></pre> <p>Build the image:</p> <pre><code>docker build -t my-arm-ci-env .\n</code></pre> <p>Pin the provisioner version for reproducible builds:</p> <pre><code>docker build --build-arg PROVISIONER_VERSION=1.2.3 -t my-arm-ci-env .\n</code></pre>"},{"location":"installing/in-docker/#architecture-aware-builds","title":"Architecture-aware builds","text":"<p>The Dockerfile above auto-detects the build architecture via <code>dpkg --print-architecture</code>. Your blueprint should use <code>per_arch</code> overrides to supply the correct toolchain URL for each architecture:</p> <pre><code># blueprint/10-toolchain.yml\n- name: Install ARM GCC toolchain\n  action: unarchive_from_url\n  dest: /opt/arm-none-eabi\n  creates: /opt/arm-none-eabi/bin/arm-none-eabi-gcc\n  per_arch:\n    amd64:\n      url: https://example.com/arm-gcc-13-amd64.tar.xz\n      sha256: \"aaa111...\"\n    arm64:\n      url: https://example.com/arm-gcc-13-arm64.tar.xz\n      sha256: \"bbb222...\"\n</code></pre> <p>This produces correct images when building on both <code>linux/amd64</code> and <code>linux/arm64</code> hosts, or when using Docker BuildKit multi-platform builds.</p>"},{"location":"installing/in-docker/#passing-variables-and-credentials","title":"Passing variables and credentials","text":"<p>If your blueprint uses <code>required_env:</code> or environment variables (for private registries, credentials, or environment-specific settings), pass them as Docker build secrets or <code>--build-arg</code>:</p> <pre><code># Pass as environment variable (visible in docker inspect \u2014 use secrets for sensitive values)\ndocker build --build-arg GITLAB_TOKEN=xxx -t my-env .\n</code></pre> <p>For sensitive values, use Docker BuildKit secrets:</p> <pre><code>RUN --mount=type=secret,id=gitlab_token \\\n    GITLAB_TOKEN=$(cat /run/secrets/gitlab_token) \\\n    alloy-provisioner --blueprint-dir /tmp/blueprint \\\n    &amp;&amp; rm -rf /tmp/blueprint\n</code></pre> <pre><code>docker build --secret id=gitlab_token,src=.env -t my-env .\n</code></pre>"},{"location":"installing/in-docker/#ci-integration","title":"CI integration","text":"<p>Use the built image in GitHub Actions:</p> <pre><code># .github/workflows/build.yml\njobs:\n  build:\n    runs-on: ubuntu-latest\n    container:\n      image: ghcr.io/your-org/my-arm-ci-env:latest\n    steps:\n      - uses: actions/checkout@v4\n      - name: Build firmware\n        run: |\n          cmake -GNinja -DCMAKE_TOOLCHAIN_FILE=cmake/arm-none-eabi.cmake -B build\n          ninja -C build\n</code></pre> <p>Or build the image as part of CI and push it to a registry:</p> <pre><code>jobs:\n  build-image:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - name: Build CI image\n        run: docker build -t ghcr.io/your-org/my-arm-ci-env:latest .\n      - name: Push\n        run: docker push ghcr.io/your-org/my-arm-ci-env:latest\n</code></pre>"},{"location":"installing/in-docker/#comparing-with-developer-vms","title":"Comparing with developer VMs","text":"<p>The same blueprint directory works for both Docker CI images and developer VMs managed by alloy-host. No duplication:</p> Context How provisioner is invoked Docker build <code>RUN alloy-provisioner --blueprint-dir /tmp/blueprint</code> alloy-host VM Bootstrap script runs <code>alloy-provisioner --blueprint-dir /config</code> Native Linux <code>alloy-provisioner --blueprint-dir /path/to/blueprint</code> <p>All three produce the same environment. Developers and CI use identical tool versions.</p>"},{"location":"installing/in-docker/#next-steps","title":"Next steps","text":"<ul> <li>Write your own blueprint</li> <li>The layers approach: keep your Dockerfile-replacing blueprint maintainable</li> <li>Variables &amp; configuration: credentials, env files, and required variables</li> </ul>"},{"location":"installing/native/","title":"On Linux (Native)","text":"<p>Install alloy-provisioner directly on any Linux machine: bare metal, a test rack, a CI server, or any existing VM you already manage (VirtualBox, Proxmox, Vagrant, WSL2, etc.). No wrapper tool needed.</p>"},{"location":"installing/native/#install-alloy-provisioner","title":"Install alloy-provisioner","text":"<p>Download and install the <code>.deb</code> package from alloy-provisioner-releases:</p> amd64arm64Auto-detect arch <pre><code>wget -q -O /tmp/alloy-provisioner.deb \\\n  https://github.com/alloy-it/alloy-provisioner-releases/releases/download/latest/alloy-provisioner_latest_linux_amd64.deb\nsudo apt-get install -y /tmp/alloy-provisioner.deb\nrm /tmp/alloy-provisioner.deb\n</code></pre> <pre><code>wget -q -O /tmp/alloy-provisioner.deb \\\n  https://github.com/alloy-it/alloy-provisioner-releases/releases/download/latest/alloy-provisioner_latest_linux_arm64.deb\nsudo apt-get install -y /tmp/alloy-provisioner.deb\nrm /tmp/alloy-provisioner.deb\n</code></pre> <pre><code>ARCH=$(dpkg --print-architecture)\nwget -q -O /tmp/alloy-provisioner.deb \\\n  \"https://github.com/alloy-it/alloy-provisioner-releases/releases/download/latest/alloy-provisioner_latest_linux_${ARCH}.deb\"\nsudo apt-get install -y /tmp/alloy-provisioner.deb\nrm /tmp/alloy-provisioner.deb\n</code></pre> <p>Verify the installation:</p> <pre><code>```bash\nalloy-provisioner -version\n```\n</code></pre>"},{"location":"installing/native/#option-a-install-from-alloy-hub","title":"Option A: Install from Alloy Hub","text":"<p>Pull a blueprint from Alloy Hub and provision in one step:</p> <pre><code>```bash\nsudo alloy-provisioner install community/arm-none-eabi\n```\n</code></pre> <p>To install a specific version:</p> <pre><code>```bash\nsudo alloy-provisioner install community/arm-none-eabi:1.2.0\n```\n</code></pre> <p>The provisioner downloads the blueprint, then executes all tasks. It prints progress as it goes and records completed tasks so re-runs skip what is already done.</p>"},{"location":"installing/native/#option-b-provision-from-a-local-blueprint","title":"Option B: Provision from a local blueprint","text":"<p>If you have a blueprint directory (e.g. committed alongside your project):</p> <pre><code>```bash\nsudo alloy-provisioner --blueprint-dir /path/to/blueprint\n```\n</code></pre> <p>Or set the directory via environment variable:</p> <pre><code>```bash\nexport ALLOY_BLUEPRINT_DIR=/path/to/blueprint\nsudo -E alloy-provisioner\n```\n</code></pre>"},{"location":"installing/native/#environment-variables-and-credentials","title":"Environment variables and credentials","text":"<p>Blueprint tasks can use environment variables for credentials, usernames, or environment-specific settings. Set them before running the provisioner using an env file:</p> <pre><code>```bash\n# Create an env file (never commit this file)\ncat &gt; ~/.alloy-env &lt;&lt; 'EOF'\nGITLAB_TOKEN=glpat-xxxxx\nSDK_DESTINATION=/opt/sdk\nDEV_USERNAME=ubuntu\nEOF\nchmod 600 ~/.alloy-env\n\n# Source and run\nset -a &amp;&amp; source ~/.alloy-env &amp;&amp; set +a\nsudo -E alloy-provisioner --blueprint-dir /path/to/blueprint\n```\n</code></pre> <p>Or use the <code>-env-file</code> flag directly:</p> <pre><code>```bash\nsudo alloy-provisioner --blueprint-dir /path/to/blueprint -env-file ~/.alloy-env\n```\n</code></pre> <p>Never commit <code>.env</code> files</p> <p>Env files contain credentials. Add them to <code>.gitignore</code>. Use CI/CD secret management for pipeline runners.</p> <p>If the blueprint declares <code>required_env:</code> in <code>manifest.yml</code>, the provisioner validates all required variables are set before running and exits with a clear error if any are missing.</p>"},{"location":"installing/native/#re-provisioning","title":"Re-provisioning","text":"<p>Running the provisioner again is always safe. It uses three layers of idempotency:</p> <ol> <li><code>creates:</code> guard: if the output path exists, the task is skipped immediately.</li> <li>State file: <code>alloy.state.yml</code> records a fingerprint of each completed task; tasks with unchanged inputs are skipped.</li> <li>Adapter-level checks: e.g. <code>apt_install</code> only installs packages that are not already present.</li> </ol> <p>To force all tasks to re-run:</p> <pre><code>```bash\nrm alloy.state.yml\nsudo alloy-provisioner --blueprint-dir /path/to/blueprint\n```\n</code></pre>"},{"location":"installing/native/#next-steps","title":"Next steps","text":"<ul> <li>Browse blueprints on Alloy Hub</li> <li>Write your own blueprint</li> <li>Variable and env file reference</li> </ul>"},{"location":"installing/with-alloy-host/","title":"With Alloy Host","text":"<p>alloy-host is a helper tool for developer workstations. It manages the lifecycle of isolated build environments (VMs and Docker containers) so that alloy-provisioner can run inside them, without touching your host machine.</p> <p>Use alloy-host when you want to:</p> <ul> <li>Keep your host clean and switch between project environments easily.</li> <li>Test a blueprint before publishing to Alloy Hub, without manually setting up a VM.</li> <li>Give teammates a one-command environment setup.</li> </ul> <p>Note</p> <p>alloy-host is a convenience wrapper. Any Linux VM you manage yourself (Vagrant, VirtualBox, Proxmox, etc.) can run alloy-provisioner natively. alloy-host is not required.</p>"},{"location":"installing/with-alloy-host/#install-alloy-host","title":"Install alloy-host","text":"macOS (Homebrew)LinuxWindows <pre><code>brew install alloy-it/tap/alloy-host\n</code></pre> <pre><code># amd64\ncurl -fsSL https://github.com/alloy-it/alloy-host-releases/releases/download/latest/alloy-host_linux_amd64.tar.gz \\\n  | tar -xz -C /usr/local/bin alloy-host\n\n# arm64\ncurl -fsSL https://github.com/alloy-it/alloy-host-releases/releases/download/latest/alloy-host_linux_arm64.tar.gz \\\n  | tar -xz -C /usr/local/bin alloy-host\n</code></pre> <p>Download <code>alloy-host_windows_amd64.zip</code> from alloy-host-releases, extract, and add to your <code>PATH</code>.</p> <p>Verify:</p> <pre><code>```bash\nalloy-host check-health\n```\n</code></pre> <p>This confirms alloy-host can find the required backend tools.</p>"},{"location":"installing/with-alloy-host/#choose-a-backend","title":"Choose a backend","text":"<p>alloy-host supports three backends:</p> Backend Flag Platforms Notes VirtualBox + Vagrant <code>--backend vm</code> (default) Windows, macOS, Linux Requires VirtualBox and Vagrant installed WSL2 <code>--backend wsl2</code> Windows Fast; requires WSL2 enabled Docker <code>--backend docker</code> All Requires Docker installed <p>VirtualBox + Vagrant prerequisites (default backend):</p> <ul> <li>macOS / Linux: install VirtualBox and Vagrant</li> <li>Windows: same, or use WSL2 instead</li> <li>Apple Silicon (M1/M2/M3): requires VirtualBox 7.1 or later</li> </ul>"},{"location":"installing/with-alloy-host/#create-your-first-environment","title":"Create your first environment","text":""},{"location":"installing/with-alloy-host/#1-initialize","title":"1. Initialize","text":"<pre><code>```bash\nalloy-host init arm-dev --blueprint community/arm-none-eabi\n```\n</code></pre> <p>This creates an <code>arm-dev/</code> directory with the backend configuration and blueprint reference. The VM does not start yet.</p> <p>To use a local blueprint instead:</p> <pre><code>```bash\nalloy-host init arm-dev --blueprint ./path/to/blueprint\n```\n</code></pre> <p>To use WSL2 or Docker:</p> <pre><code>```bash\nalloy-host init arm-dev --blueprint community/arm-none-eabi --backend wsl2\nalloy-host init arm-dev --blueprint community/arm-none-eabi --backend docker\n```\n</code></pre>"},{"location":"installing/with-alloy-host/#2-start-and-provision","title":"2. Start and provision","text":"<pre><code>```bash\ncd arm-dev\nalloy-host up\n```\n</code></pre> <p>alloy-host starts the backend, installs alloy-provisioner inside the guest, and runs the blueprint. You'll see each task execute in sequence. The first run downloads toolchains; this takes a few minutes.</p> <pre><code>```toml\nBringing machine up...\nRunning provisioner...\n\u2714  Update APT cache\n\u2714  Install build tools\n\u2714  Install ARM GCC toolchain\n\u2714  Configure PATH\nProvisioning complete.\n```\n</code></pre> <p>Running <code>alloy-host up</code> again is safe; completed tasks are skipped.</p>"},{"location":"installing/with-alloy-host/#3-enter-the-environment","title":"3. Enter the environment","text":"<pre><code>```bash\nalloy-host ssh\n```\n</code></pre> <p>You're now inside the VM (or WSL2/Docker container). Your host project directory is mounted at <code>/vagrant</code>. Verify the toolchain:</p> <pre><code>```bash\narm-none-eabi-gcc --version\ncmake --version\nninja --version\n```\n</code></pre>"},{"location":"installing/with-alloy-host/#4-stop-when-done","title":"4. Stop when done","text":"<pre><code>```bash\nalloy-host stop\n```\n</code></pre> <p>The environment is suspended with everything intact. Resume later with <code>alloy-host up</code>.</p>"},{"location":"installing/with-alloy-host/#daily-workflow","title":"Daily workflow","text":"<pre><code>```mermaid\nflowchart LR\n    Up[alloy-host up] --&gt; SSH[alloy-host ssh]\n    SSH --&gt; Work[work inside env]\n    Work --&gt; Stop[alloy-host stop]\n```\n\n```bash\nalloy-host up        # start the environment\nalloy-host ssh       # open a shell inside it\n# ... do your work ...\nalloy-host stop      # shut it down when done\n```\n</code></pre> <p>When inside the environment directory you can omit the name. From anywhere else:</p> <pre><code>```bash\nalloy-host ssh arm-dev\nalloy-host up stm32-proj\n```\n</code></pre>"},{"location":"installing/with-alloy-host/#updating-the-environment","title":"Updating the environment","text":"<p>When your blueprint changes, re-provision the running environment:</p> <pre><code>```bash\nalloy-host provision\n```\n</code></pre> <p>Only new or changed tasks run. Everything else is skipped.</p>"},{"location":"installing/with-alloy-host/#managing-multiple-environments","title":"Managing multiple environments","text":"<pre><code>```bash\nalloy-host list\n```\n</code></pre> <p>Shows all registered environments with status, backend, and blueprint.</p>"},{"location":"installing/with-alloy-host/#destroying-and-rebuilding","title":"Destroying and rebuilding","text":"<pre><code>```bash\nalloy-host destroy       # delete the VM (project files on host are not affected)\nalloy-host up            # reprovisioned from scratch\n```\n</code></pre> <p>For a complete clean slate (also wipes the persistent data disk):</p> <pre><code>```bash\nalloy-host destroy --all\nalloy-host up\n```\n</code></pre>"},{"location":"installing/with-alloy-host/#quick-reference","title":"Quick reference","text":"Command What it does <code>alloy-host init &lt;name&gt; --blueprint &lt;bp&gt;</code> Create environment directory <code>alloy-host up [name]</code> Start or recreate the environment <code>alloy-host ssh [name]</code> Open a shell inside the environment <code>alloy-host provision [name]</code> Re-run the blueprint (apply updates) <code>alloy-host stop [name]</code> Suspend the environment <code>alloy-host destroy [name]</code> Delete the environment <code>alloy-host list</code> Show all registered environments <code>alloy-host validate [name]</code> Validate blueprint syntax <code>alloy-host resolve [name]</code> Generate <code>alloy.lock.yml</code> from catalog refs <code>alloy-host check-health</code> Verify alloy-host is correctly installed <p>Full details: Command Reference</p>"},{"location":"installing/with-alloy-host/#next-steps","title":"Next steps","text":"<ul> <li>USB device passthrough: attach development boards to your VM</li> <li>Write your own blueprint</li> <li>Sharing with your team</li> </ul>"},{"location":"reference/blueprint-syntax/","title":"Blueprint Syntax Reference","text":"<p>Complete reference for writing Alloy blueprint files.</p>"},{"location":"reference/blueprint-syntax/#directory-structure","title":"Directory structure","text":"<pre><code>blueprint/\n\u251c\u2500\u2500 manifest.yml       # required: entry point and metadata\n\u251c\u2500\u2500 00-system.yml      # task files \u2014 named and ordered as you like\n\u251c\u2500\u2500 10-toolchain.yml\n\u2514\u2500\u2500 20-sdk.yml\n</code></pre>"},{"location":"reference/blueprint-syntax/#manifestyml","title":"manifest.yml","text":"<pre><code>name: \"My Environment\" # required\nversion: \"1.0\" # required\ndescription: \"...\" # required\n\nvariables: # optional: reusable values\n  KEY: \"value\"\n  ANOTHER: \"value\"\n\ntoolchains: # optional: catalog-sourced tool references\n  - ref: \"toolchain.arm-gnu.arm-none-eabi@stable\"\n    alias: \"ARM_GCC\" # injects ARM_GCC_URL and ARM_GCC_SHA as variables\n\nsupported_hosts: # optional: list of OS/arch this blueprint targets\n  - linux/amd64\n  - linux/arm64\n\nrun_order: # required: task files to run, in order\n  - \"00-system.yml\"\n  - \"10-toolchain.yml\"\n</code></pre>"},{"location":"reference/blueprint-syntax/#variables","title":"Variables","text":"<p>Variables are referenced in task files with <code>{{.Vars.NAME}}</code>:</p> <pre><code># manifest.yml\nvariables:\n  GCC_VERSION: \"13.3.rel1\"\n  TOOLCHAIN_DIR: \"/opt/toolchains\"\n</code></pre> <pre><code># in a task file:\nurl: \"https://example.com/gcc-{{.Vars.GCC_VERSION}}.tar.xz\"\ndest: \"{{.Vars.TOOLCHAIN_DIR}}/arm-gcc\"\n</code></pre>"},{"location":"reference/blueprint-syntax/#toolchain-aliases","title":"Toolchain aliases","text":"<p>When a <code>toolchains:</code> entry has an <code>alias</code>, the resolved URL and SHA256 are injected as variables <code>{ALIAS}_URL</code> and <code>{ALIAS}_SHA</code>:</p> <pre><code>toolchains:\n  - ref: \"toolchain.arm-gnu.arm-none-eabi@stable\"\n    alias: \"ARM_GCC\"\n</code></pre> <p>This makes <code>{{.Vars.ARM_GCC_URL}}</code> and <code>{{.Vars.ARM_GCC_SHA}}</code> available in task files.</p>"},{"location":"reference/blueprint-syntax/#task-file-structure","title":"Task file structure","text":"<p>Task files are flat YAML lists. Each item is a task object. There is no wrapping key.</p> <pre><code>- name: \"Task description\" # required\n  action: \"action_name\" # required\n  # action-specific fields...\n  creates: \"/path/to/file\" # optional: idempotency guard\n  run_as: \"username\" # optional: run as this user (default: root)\n  owner: \"user:group\" # optional: set ownership after action\n  mode: \"0755\" # optional: set permissions after action\n  always_run: true # optional: skip creates check, always run\n  skip_if: \"shell expr\" # optional: skip if expression exits 0\n  exclude: [docker, wsl2] # optional: skip on named environment tags\n  per_arch: # optional: architecture-specific overrides\n    amd64:\n      url: \"...\"\n      sha256: \"...\"\n    arm64:\n      url: \"...\"\n      sha256: \"...\"\n</code></pre>"},{"location":"reference/blueprint-syntax/#creates","title":"<code>creates</code>","text":"<p>The idempotency guard. If the path exists, the task is skipped. Use it on every task that installs something:</p> <pre><code>- name: \"Install ARM GCC\"\n  action: \"unarchive_from_url\"\n  # ...\n  creates: \"/opt/toolchains/arm-gcc/bin/arm-none-eabi-gcc\"\n</code></pre>"},{"location":"reference/blueprint-syntax/#always_run","title":"<code>always_run</code>","text":"<p>Bypasses the <code>creates</code> check. Useful for tasks that should run every time, like <code>apt-get update</code>:</p> <pre><code>- name: \"Update APT cache\"\n  action: \"run_command\"\n  command: \"apt-get update -y\"\n  always_run: true\n</code></pre>"},{"location":"reference/blueprint-syntax/#skip_if","title":"<code>skip_if</code>","text":"<p>Skip a task if a shell expression exits 0:</p> <pre><code>- name: \"Reload udev rules\"\n  action: \"run_command\"\n  command: \"udevadm control --reload-rules &amp;&amp; udevadm trigger\"\n  always_run: true\n  skip_if: '[ \"$ALLOY_BACKEND\" = \"wsl2\" ]'\n</code></pre>"},{"location":"reference/blueprint-syntax/#exclude","title":"<code>exclude</code>","text":"<p>Skip a task on specific named environments. Cleaner and more readable than <code>skip_if</code> for well-known environments.</p> <p>Built-in tags:</p> Tag When active <code>docker</code> <code>--docker</code> flag or <code>ALLOY_ENV_DOCKER=1</code> env var <code>wsl2</code> <code>--wsl2</code> flag or <code>ALLOY_ENV_WSL2=1</code> env var <p>When running inside alloy-host, <code>--wsl2</code> is automatically passed by the WSL2 backend.</p> <pre><code>- name: \"Install udev rules\"\n  action: \"udev_rules\"\n  rule_file: \"99-my-device.rules\"\n  exclude: [docker, wsl2]  # udev doesn't work inside Docker containers or WSL2\n\n- name: \"Register QEMU binfmt\"\n  action: \"register_binfmt\"\n  architecture: \"arm\"\n  interpreter: \"/usr/bin/qemu-arm-static\"\n  exclude: [docker]  # binfmt registration requires privileged mode; skip in Docker\n</code></pre> <p>You can combine <code>exclude</code> with <code>skip_if</code> \u2014 both are checked independently and either condition can cause the task to be skipped.</p>"},{"location":"reference/blueprint-syntax/#run_as","title":"<code>run_as</code>","text":"<p>Run the task as a specific user instead of root:</p> <pre><code>- name: \"Install West\"\n  action: \"run_command\"\n  run_as: \"vagrant\"\n  command: \"pip3 install --user west\"\n  creates: \"/home/vagrant/.local/bin/west\"\n</code></pre>"},{"location":"reference/blueprint-syntax/#owner-and-mode","title":"<code>owner</code> and <code>mode</code>","text":"<p>Set file ownership and permissions after the action completes:</p> <pre><code>- name: \"Write udev rules\"\n  action: \"get_file\"\n  url: \"https://example.com/99-device.rules\"\n  sha256: \"abc123...\"\n  dest: \"/etc/udev/rules.d/99-device.rules\"\n  owner: \"root:root\"\n  mode: \"0644\"\n</code></pre> <p><code>mode</code> accepts octal strings (<code>\"0755\"</code>) or symbolic strings:</p> Symbolic Octal <code>USER_RWX GROUP_RX OTHER_RX</code> <code>0755</code> <code>USER_RW GROUP_R OTHER_R</code> <code>0644</code> <code>USER_RW GROUP_RW OTHER_NONE</code> <code>0660</code> <code>READ_ALL</code> <code>0444</code> <code>FULL_CONTROL</code> <code>0777</code>"},{"location":"reference/blueprint-syntax/#per_arch","title":"<code>per_arch</code>","text":"<p>Override specific fields per host architecture. Supported keys: <code>url</code>, <code>sha256</code>, <code>command</code>, <code>source</code>. Supported architectures: <code>amd64</code>, <code>arm64</code>.</p> <pre><code>- name: \"Install toolchain\"\n  action: \"unarchive_from_url\"\n  url: \"https://example.com/toolchain-x86_64.tar.xz\"\n  sha256: \"aaa111...\"\n  dest: \"/opt/toolchains/arm-gcc\"\n  strip_components: 1\n  creates: \"/opt/toolchains/arm-gcc/bin/arm-none-eabi-gcc\"\n  per_arch:\n    arm64:\n      url: \"https://example.com/toolchain-aarch64.tar.xz\"\n      sha256: \"bbb222...\"\n</code></pre>"},{"location":"reference/blueprint-syntax/#actions","title":"Actions","text":""},{"location":"reference/blueprint-syntax/#apt_install","title":"<code>apt_install</code>","text":"<p>Install packages from the Debian/Ubuntu APT repository.</p> <pre><code>- name: \"Install build tools\"\n  action: \"apt_install\"\n  packages:\n    - \"cmake\"\n    - \"ninja-build\"\n    - \"gdb-multiarch\"\n    - \"python3\"\n    - \"git\"\n</code></pre>"},{"location":"reference/blueprint-syntax/#run_command","title":"<code>run_command</code>","text":"<p>Run a shell command. <code>reason</code> is required: it must explain why the task cannot be expressed as one of the dedicated actions. This creates an audit trail and encourages replacing one-off commands with proper primitives over time.</p> Field Required Description <code>command</code> Yes Shell command to run <code>reason</code> Yes Why no dedicated action covers this step <code>run_as</code> No Run as this user (default: root) <code>always_run</code> No Skip <code>creates</code> check and always run <code>skip_if</code> No Shell expression; skip if it exits 0 <code>creates</code> No Skip if this path already exists <pre><code>- name: \"Update APT cache\"\n  action: \"run_command\"\n  reason: \"apt-get update has no dedicated primitive; must run before any apt_install step.\"\n  command: \"apt-get update -y\"\n  always_run: true\n</code></pre> <p>Multi-line:</p> <pre><code>- name: \"Export Zephyr CMake packages\"\n  action: \"run_command\"\n  reason: \"west zephyr-export registers CMake package paths; no dedicated primitive covers CMake package registration.\"\n  run_as: \"vagrant\"\n  command: |\n    export PATH=$HOME/.local/bin:$PATH\n    cd /opt/ncs/v2.9.0 &amp;&amp; west zephyr-export\n  creates: \"/opt/ncs/v2.9.0/.zephyr_exported\"\n</code></pre>"},{"location":"reference/blueprint-syntax/#get_file","title":"<code>get_file</code>","text":"<p>Download a single file to a path.</p> <pre><code>- name: \"Download udev rules\"\n  action: \"get_file\"\n  url: \"https://example.com/99-device.rules\"\n  sha256: \"abc123...\"\n  dest: \"/etc/udev/rules.d/99-device.rules\"\n  creates: \"/etc/udev/rules.d/99-device.rules\"\n</code></pre> <p>Supports <code>per_arch</code> for architecture-specific URLs.</p>"},{"location":"reference/blueprint-syntax/#unarchive_from_url","title":"<code>unarchive_from_url</code>","text":"<p>Download and extract an archive (<code>.tar.gz</code>, <code>.tar.xz</code>, <code>.tar.bz2</code>, <code>.zip</code>).</p> <pre><code>- name: \"Install ARM GCC\"\n  action: \"unarchive_from_url\"\n  url: \"https://developer.arm.com/.../arm-gnu-toolchain-13.3.rel1-x86_64-arm-none-eabi.tar.xz\"\n  sha256: \"6cd1bbc1d9ae57408d592354e4f59a82a2427e86a35cf2b97209c9b67c4b3ac6\"\n  dest: \"/opt/toolchains/arm-gcc\"\n  strip_components: 1\n  creates: \"/opt/toolchains/arm-gcc/bin/arm-none-eabi-gcc\"\n  mode: \"USER_RWX GROUP_RX OTHER_RX\"\n</code></pre> <p><code>strip_components</code> strips N leading directory levels from paths when extracting.</p>"},{"location":"reference/blueprint-syntax/#unarchive_from_ref","title":"<code>unarchive_from_ref</code>","text":"<p>Download and extract a versioned tool from the Alloy catalog.</p> <pre><code>- name: \"Install ARM GNU toolchain from catalog\"\n  action: \"unarchive_from_ref\"\n  ref: \"toolchain.arm-gnu.arm-none-eabi@stable\"\n  dest: \"/opt/toolchains/arm-gcc\"\n  strip_components: 1\n  creates: \"/opt/toolchains/arm-gcc/bin/arm-none-eabi-gcc\"\n  mode: \"USER_RWX GROUP_RX OTHER_RX\"\n</code></pre> <p>Ref format: <code>&lt;type&gt;.&lt;vendor&gt;.&lt;tool&gt;@&lt;version-or-alias&gt;</code>. Run <code>alloy-host catalog info &lt;ref&gt;</code> to see available versions.</p>"},{"location":"reference/blueprint-syntax/#write_env_file","title":"<code>write_env_file</code>","text":"<p>Write arbitrary content to a file with variable substitution. Used for shell profile scripts, CMake toolchain files, config files.</p> <pre><code>- name: \"Add toolchain to PATH\"\n  action: \"write_env_file\"\n  file: \"/etc/profile.d/arm-gcc.sh\"\n  content: |\n    #!/bin/sh\n    export PATH=$PATH:{{.Vars.TOOLCHAIN_DIR}}/arm-gcc/bin\n    export ARM_GCC_ROOT={{.Vars.TOOLCHAIN_DIR}}/arm-gcc\n  mode: \"USER_RWX GROUP_RX OTHER_RX\"\n</code></pre> <pre><code>- name: \"Create CMake toolchain file\"\n  action: \"write_env_file\"\n  file: \"/opt/cmake/arm-none-eabi.cmake\"\n  content: |\n    set(CMAKE_SYSTEM_NAME Generic)\n    set(CMAKE_C_COMPILER arm-none-eabi-gcc)\n    set(CMAKE_CXX_COMPILER arm-none-eabi-g++)\n  mode: \"USER_RW GROUP_R OTHER_R\"\n</code></pre>"},{"location":"reference/blueprint-syntax/#unpack","title":"<code>unpack</code>","text":"<p>Extract a local archive already present inside the VM or at <code>/vagrant</code>.</p> <pre><code>- name: \"Unpack bundled tools\"\n  action: \"unpack\"\n  source: \"/vagrant/tools/my-tool.tar.gz\"\n  dest: \"/opt/my-tool\"\n  creates: \"/opt/my-tool/bin/tool\"\n</code></pre>"},{"location":"reference/blueprint-syntax/#install_target_lib","title":"<code>install_target_lib</code>","text":"<p>Install a package (e.g. a <code>.deb</code>) into a sysroot directory. Used for cross-compilation setups that need target-architecture libraries.</p> <pre><code>- name: \"Install target library into sysroot\"\n  action: \"install_target_lib\"\n  url: \"https://example.com/libfoo-armhf.deb\"\n  sha256: \"abc123...\"\n  sysroot: \"/opt/sysroot\"\n  creates: \"/opt/sysroot/usr/lib/libfoo.so\"\n</code></pre>"},{"location":"reference/blueprint-syntax/#python_package","title":"<code>python_package</code>","text":"<p>Install a Python package via <code>pip3</code>. Use this instead of <code>run_command: pip3 install ...</code>.</p> Field Required Description <code>package</code> Yes PyPI package name <code>version</code> No Exact version to install (e.g. <code>\"1.3.0\"</code>) <pre><code>- name: \"Install West\"\n  action: \"python_package\"\n  package: \"west\"\n  version: \"{{.Vars.WEST_VERSION}}\"\n  creates: \"/home/{{.Vars.DEV_USERNAME}}/.local/bin/west\"\n</code></pre>"},{"location":"reference/blueprint-syntax/#rust_package","title":"<code>rust_package</code>","text":"<p>Install a Rust crate via <code>cargo install</code>. <code>cargo</code> must already be on <code>PATH</code>.</p> Field Required Description <code>package</code> Yes Crate name on crates.io <code>version</code> Yes Exact version to install <code>features</code> No List of Cargo features to enable <pre><code>- name: \"Install cargo-binutils\"\n  action: \"rust_package\"\n  package: \"cargo-binutils\"\n  version: \"0.3.6\"\n</code></pre>"},{"location":"reference/blueprint-syntax/#mcu_sdk","title":"<code>mcu_sdk</code>","text":"<p>Install a vendor MCU SDK using its native tooling. The install tool (e.g. <code>west</code>) must already be on <code>PATH</code>; install it first with <code>python_package</code>.</p> Field Required Description <code>method</code> Yes Install method: <code>west</code> or <code>git</code> <code>url</code> Yes Repository URL <code>version</code> Yes Tag or branch to check out <code>dest</code> Yes Installation directory <code>run_as</code> No Run as this user (default: root) <code>creates</code> No Skip if this path already exists <pre><code>- name: \"Initialize nRF Connect SDK\"\n  action: \"mcu_sdk\"\n  method: \"west\"\n  url: \"{{.Vars.NCS_URL}}\"\n  version: \"{{.Vars.NCS_VERSION}}\"\n  dest: \"{{.Vars.NCS_BASE}}/{{.Vars.NCS_VERSION}}\"\n  run_as: \"{{.Vars.DEV_USERNAME}}\"\n  creates: \"{{.Vars.NCS_BASE}}/{{.Vars.NCS_VERSION}}/nrf\"\n</code></pre>"},{"location":"reference/blueprint-syntax/#cmake_toolchain","title":"<code>cmake_toolchain</code>","text":"<p>Generate a CMake toolchain file from declared cross-compilation parameters. Prefer this over <code>write_env_file</code> for <code>.cmake</code> files; the provisioner validates all required fields.</p> Field Required Description <code>file</code> Yes Output path for the <code>.cmake</code> file <code>target_system</code> Yes <code>CMAKE_SYSTEM_NAME</code> (e.g. <code>Generic</code>, <code>Linux</code>) <code>target_processor</code> Yes <code>CMAKE_SYSTEM_PROCESSOR</code> (e.g. <code>arm</code>, <code>aarch64</code>) <code>compiler_prefix</code> Yes Toolchain prefix (e.g. <code>arm-none-eabi</code>) <code>compiler_path</code> Yes Directory containing the compiler binaries <code>sysroot</code> No <code>CMAKE_SYSROOT</code> path <pre><code>- name: \"Generate ARM CMake toolchain\"\n  action: \"cmake_toolchain\"\n  file: \"/opt/cmake/arm-none-eabi.cmake\"\n  target_system: \"Generic\"\n  target_processor: \"arm\"\n  compiler_prefix: \"arm-none-eabi\"\n  compiler_path: \"{{.Vars.ARM_TOOLCHAIN_DEST}}/bin\"\n</code></pre>"},{"location":"reference/blueprint-syntax/#install_deb_to_sysroot","title":"<code>install_deb_to_sysroot</code>","text":"<p>Install a <code>.deb</code> package into a cross-compilation sysroot (not the host system). The formalized version of <code>install_target_lib</code> that takes <code>sysroot</code> explicitly.</p> Field Required Description <code>url</code> Yes URL of the <code>.deb</code> file (supports <code>per_arch</code>) <code>sha256</code> Yes SHA256 of the <code>.deb</code> file <code>sysroot</code> Yes Sysroot directory to install into <pre><code>- name: \"Install libfoo for ARM\"\n  action: \"install_deb_to_sysroot\"\n  url: \"https://apt.example.com/libfoo_1.0_armhf.deb\"\n  sha256: \"abc123...\"\n  sysroot: \"{{.Vars.SDK_SYSROOT}}\"\n</code></pre>"},{"location":"reference/blueprint-syntax/#register_binfmt","title":"<code>register_binfmt</code>","text":"<p>Register a QEMU interpreter for transparent cross-architecture execution via <code>binfmt_misc</code>. Use this instead of <code>run_command: update-binfmts ...</code>.</p> Field Required Description <code>architecture</code> Yes Target arch: <code>arm</code>, <code>aarch64</code>, <code>riscv64</code>, etc. <code>interpreter</code> Yes Path to the QEMU static binary <code>package</code> No APT package to install first (e.g. <code>qemu-user-static</code>) <pre><code>- name: \"Register ARM binfmt for cross-build\"\n  action: \"register_binfmt\"\n  architecture: \"arm\"\n  interpreter: \"/usr/bin/qemu-arm-static\"\n  package: \"qemu-user-static\"\n</code></pre>"},{"location":"reference/blueprint-syntax/#sdk_install","title":"<code>sdk_install</code>","text":"<p>Run a self-extracting SDK installer script (Yocto eSDK / Poky pattern). Downloads, verifies SHA256, and executes the installer.</p> Field Required Description <code>url</code> Yes URL of the installer script <code>sha256</code> Yes SHA256 of the installer script <code>dest</code> Yes Installation directory <pre><code>- name: \"Install Yocto eSDK\"\n  action: \"sdk_install\"\n  url: \"https://example.com/poky-toolchain-4.0.sh\"\n  sha256: \"abc123...\"\n  dest: \"/opt/poky/4.0\"\n  creates: \"/opt/poky/4.0/environment-setup-cortexa8hf-neon-poky-linux-gnueabi\"\n</code></pre>"},{"location":"reference/blueprint-syntax/#build_artifact","title":"<code>build_artifact</code>","text":"<p>Download, verify, and build a source tarball using <code>./configure</code> + <code>make</code>. Use this instead of chaining multiple <code>run_command</code> tasks for source builds.</p> Field Required Description <code>artifact_source</code> Yes Nested block: <code>url</code> and <code>sha256</code> of the source tarball <code>make_targets</code> Yes List of <code>make</code> targets (e.g. <code>[\"all\", \"install\"]</code>) <code>dependencies</code> No APT packages to install before building <code>configure_flags</code> No Arguments for <code>./configure</code> <code>destination</code> No Install prefix <pre><code>- name: \"Build libusb from source\"\n  action: \"build_artifact\"\n  artifact_source:\n    url: \"https://github.com/libusb/libusb/releases/download/v1.0.27/libusb-1.0.27.tar.bz2\"\n    sha256: \"abc123...\"\n  dependencies:\n    - \"libudev-dev\"\n  configure_flags:\n    - \"--prefix=/usr\"\n    - \"--disable-static\"\n  make_targets:\n    - \"all\"\n    - \"install\"\n</code></pre>"},{"location":"reference/blueprint-syntax/#udev_rules","title":"<code>udev_rules</code>","text":"<p>Download a udev rules file and install it into <code>/etc/udev/rules.d/</code>, then reload udev. Use this instead of <code>write_env_file</code> for rules sourced from a URL.</p> Field Required Description <code>url</code> Yes URL of the rules file <code>sha256</code> Yes SHA256 of the rules file <code>rule_file</code> Yes Filename to use in <code>/etc/udev/rules.d/</code> <pre><code>- name: \"Install Nordic Semiconductor udev rules\"\n  action: \"udev_rules\"\n  url: \"https://raw.githubusercontent.com/NordicSemiconductor/nrf-udev/main/nrf-udev_1.0.1-all.deb\"\n  sha256: \"abc123...\"\n  rule_file: \"99-nordic.rules\"\n</code></pre>"},{"location":"reference/blueprint-syntax/#yocto_manifest","title":"<code>yocto_manifest</code>","text":"<p>Check out a Yocto layer configuration using <code>kas</code>. Install <code>kas</code> first with <code>python_package</code>.</p> Field Required Description <code>url</code> Yes URL of the kas config repository <code>dest</code> Yes Directory to check out into <code>commit_hash</code> No Git commit SHA to pin to <code>kas_file</code> No Path to the kas config YAML inside the repo <pre><code>- name: \"Configure Yocto layers\"\n  action: \"yocto_manifest\"\n  url: \"https://github.com/myorg/my-kas-config.git\"\n  dest: \"/opt/yocto/build\"\n  commit_hash: \"abc123def456...\"\n  kas_file: \"kas/my-machine.yml\"\n</code></pre>"},{"location":"reference/blueprint-syntax/#buildroot_config","title":"<code>buildroot_config</code>","text":"<p>Clone Buildroot at a specific release and apply a defconfig. <code>git</code> must be on <code>PATH</code>.</p> Field Required Description <code>url</code> Yes Buildroot git repository URL <code>dest</code> Yes Directory to clone into <code>version</code> No Tag or branch (e.g. <code>\"2024.02\"</code>) <code>defconfig</code> No Defconfig name to apply <code>external_tree</code> No URL of a Buildroot external tree to also clone <pre><code>- name: \"Set up Buildroot\"\n  action: \"buildroot_config\"\n  url: \"https://git.buildroot.net/buildroot\"\n  dest: \"/opt/buildroot\"\n  version: \"2024.02\"\n  defconfig: \"raspberrypi4_defconfig\"\n</code></pre>"},{"location":"reference/blueprint-syntax/#pkg_config_env","title":"<code>pkg_config_env</code>","text":"<p>Generate a shell environment script that configures <code>pkg-config</code> to search a cross-compilation sysroot. Use after <code>install_deb_to_sysroot</code> or <code>sdk_install</code>.</p> Field Required Description <code>sysroot</code> Yes Cross-compilation sysroot directory <code>env_file</code> Yes Output path (e.g. <code>/etc/profile.d/50-pkgconfig.sh</code>) <pre><code>- name: \"Configure pkg-config for ARM sysroot\"\n  action: \"pkg_config_env\"\n  sysroot: \"{{.Vars.SDK_SYSROOT}}\"\n  env_file: \"/etc/profile.d/50-pkgconfig.sh\"\n</code></pre>"},{"location":"reference/blueprint-syntax/#lockfile-alloylockyml","title":"Lockfile (alloy.lock.yml)","text":"<p>Generated by <code>alloy-host resolve</code>. Records the exact download URLs and SHA256 checksums for every catalog-sourced tool, resolved for your host architecture. Commit this file.</p> <pre><code># Generated by alloy-host resolve. Commit this file.\nresolved_at: \"2026-01-15T10:30:00Z\"\nhost: \"linux/amd64\"\ntoolchains:\n  toolchain.arm-gnu.arm-none-eabi:\n    url: \"https://developer.arm.com/downloads/...\"\n    sha256: \"6cd1bbc1d9ae57408d592354e4f59a82a2427e86a35cf2b97209c9b67c4b3ac6\"\n    method: \"archive\"\n</code></pre> <p>Regenerate after changing <code>toolchains:</code> entries: <code>alloy-host resolve</code></p>"},{"location":"reference/commands/","title":"Command Reference","text":"<p>Complete reference for all <code>alloy-host</code> commands.</p> <p>Most commands accept an optional environment name. If omitted, Alloy uses the environment in the current directory, or prompts you to select if multiple are registered.</p> <p>Architecture: <code>alloy-host</code> runs on your machine (the host). It creates and manages VMs (or WSL2 instances) and copies the blueprint into each environment. The actual provisioning (installing packages, downloading toolchains, writing env files) is performed inside the guest by alloy-provisioner, which alloy-host triggers via the guest's bootstrap script. See How Alloy works for the full model.</p>"},{"location":"reference/commands/#environment-lifecycle","title":"Environment lifecycle","text":""},{"location":"reference/commands/#alloy-host-init","title":"<code>alloy-host init</code>","text":"<p>Create a new environment. The environment directory is placed at <code>~/.alloy-it/envs/&lt;name&gt;/</code> by default.</p> <pre><code>alloy-host init &lt;name&gt; --blueprint &lt;blueprint-name&gt; [flags]\n</code></pre> <p><code>--blueprint</code> is required.</p> Flag Description <code>--blueprint</code>, <code>-b</code> Blueprint name to use (required) <code>--backend</code> Backend type: <code>vm</code> (default) or <code>wsl2</code> <code>--wsl-user</code> Linux username inside the WSL2 distro (WSL2 only, default: <code>vagrant</code>) <code>--path</code> Override base directory for the environment (default: <code>~/.alloy-it/envs/</code>) <p>Examples:</p> <pre><code>alloy-host init arm-dev --blueprint arm-gcc\nalloy-host init nrf91-dev --blueprint nrf91\nalloy-host init arm-dev --blueprint arm-gcc --backend wsl2\nalloy-host init arm-dev --blueprint arm-gcc --path ~/projects/embedded\n</code></pre> <p>Init registers the environment but does not start the VM.</p>"},{"location":"reference/commands/#alloy-host-up","title":"<code>alloy-host up</code>","text":"<p>Start the VM. Creates and provisions it if it doesn't exist yet; resumes it if it's stopped.</p> <pre><code>alloy-host up [name]\n</code></pre> <p>Examples:</p> <pre><code>cd arm-dev &amp;&amp; alloy-host up\nalloy-host up arm-dev\n</code></pre>"},{"location":"reference/commands/#alloy-host-stop","title":"<code>alloy-host stop</code>","text":"<p>Suspend the VM. Data and state are preserved. Resume with <code>up</code>.</p> <pre><code>alloy-host stop [name]\n</code></pre>"},{"location":"reference/commands/#alloy-host-destroy","title":"<code>alloy-host destroy</code>","text":"<p>Delete the VM. The environment directory and its files are not affected; recreate the VM any time with <code>alloy-host up</code>.</p> <pre><code>alloy-host destroy [name] [flags]\n</code></pre> Flag Description <code>--all</code>, <code>-a</code> Also delete the persistent data disk. Permanent."},{"location":"reference/commands/#alloy-host-provision","title":"<code>alloy-host provision</code>","text":"<p>Re-run the blueprint on an already-running VM. Tasks that previously completed are skipped. Use this after modifying the blueprint.</p> <pre><code>alloy-host provision [name]\n</code></pre>"},{"location":"reference/commands/#alloy-host-list","title":"<code>alloy-host list</code>","text":"<p>Show all registered environments.</p> <pre><code>alloy-host list\n</code></pre>"},{"location":"reference/commands/#working-with-the-vm","title":"Working with the VM","text":""},{"location":"reference/commands/#alloy-host-ssh","title":"<code>alloy-host ssh</code>","text":"<p>Open an interactive shell inside the VM. Your host project directory is available at <code>/vagrant</code>.</p> <pre><code>alloy-host ssh [name]\n</code></pre>"},{"location":"reference/commands/#blueprint-management","title":"Blueprint management","text":""},{"location":"reference/commands/#alloy-host-validate","title":"<code>alloy-host validate</code>","text":"<p>Check blueprint YAML files for syntax errors and missing required fields, without touching the VM.</p> <pre><code>alloy-host validate [name|path]\n</code></pre> <pre><code>alloy-host validate\nalloy-host validate arm-dev\nalloy-host validate /path/to/blueprint-dir\n</code></pre>"},{"location":"reference/commands/#alloy-host-resolve","title":"<code>alloy-host resolve</code>","text":"<p>Read <code>toolchains:</code> references from <code>manifest.yml</code>, resolve them against the local catalog, and write <code>alloy.lock.yml</code> with the exact URLs and checksums for your host architecture.</p> <pre><code>alloy-host resolve [name]\n</code></pre> <p>Run this after adding or changing <code>toolchains:</code> entries in your manifest. Commit the resulting <code>alloy.lock.yml</code>.</p> <pre><code>cd arm-dev &amp;&amp; alloy-host resolve\n</code></pre>"},{"location":"reference/commands/#usb-device-management","title":"USB device management","text":""},{"location":"reference/commands/#alloy-host-usb-list","title":"<code>alloy-host usb list</code>","text":"<p>List USB devices connected to the host. Pass a VM name to see which are already attached to it.</p> <pre><code>alloy-host usb list [name]\n</code></pre>"},{"location":"reference/commands/#alloy-host-usb-attach","title":"<code>alloy-host usb attach</code>","text":"<p>Attach a USB device to a VM. Identify the device by number (from <code>usb list</code>), partial name, or UUID/BUSID.</p> <pre><code>alloy-host usb attach [device] [name]\n</code></pre> <p>Examples:</p> <pre><code>alloy-host usb attach                  # interactive: pick VM then device\nalloy-host usb attach 1                # device 1 to current directory's VM\nalloy-host usb attach 1 arm-dev        # device 1 to arm-dev\nalloy-host usb attach \"J-Link\" arm-dev # by partial name\n</code></pre>"},{"location":"reference/commands/#alloy-host-usb-detach","title":"<code>alloy-host usb detach</code>","text":"<p>Release a USB device back to the host.</p> <pre><code>alloy-host usb detach &lt;device&gt; [name]\n</code></pre> <pre><code>alloy-host usb detach 1\nalloy-host usb detach 1 arm-dev\nalloy-host usb detach \"J-Link\" arm-dev\n</code></pre>"},{"location":"reference/commands/#alloy-host-usb-status","title":"<code>alloy-host usb status</code>","text":"<p>Show which USB devices are currently attached to any VM or WSL2.</p> <pre><code>alloy-host usb status\n</code></pre>"},{"location":"reference/commands/#catalog","title":"Catalog","text":""},{"location":"reference/commands/#alloy-host-catalog-update","title":"<code>alloy-host catalog update</code>","text":"<p>Clone or pull the latest catalog into <code>~/.alloy-it/catalog/</code>.</p> <pre><code>alloy-host catalog update [flags]\n</code></pre> Flag Description <code>--remote</code> Remote Git URL (default: public Alloy catalog)"},{"location":"reference/commands/#alloy-host-catalog-search","title":"<code>alloy-host catalog search</code>","text":"<p>Search the local catalog by ID, name, or tag.</p> <pre><code>alloy-host catalog search &lt;query&gt;\n</code></pre> <pre><code>alloy-host catalog search arm\nalloy-host catalog search nordic\nalloy-host catalog search golang\n</code></pre>"},{"location":"reference/commands/#alloy-host-catalog-info","title":"<code>alloy-host catalog info</code>","text":"<p>Show details about a catalog entry, including all available versions.</p> <pre><code>alloy-host catalog info &lt;ref&gt;\n</code></pre> <pre><code>alloy-host catalog info toolchain.arm-gnu.arm-none-eabi\nalloy-host catalog info toolchain.arm-gnu.arm-none-eabi@13.3.rel1\n</code></pre>"},{"location":"reference/commands/#alloy-host-catalog-add","title":"<code>alloy-host catalog add</code>","text":"<p>Validate a version descriptor YAML file and update the tool's <code>index.yaml</code>. Used when contributing to the catalog.</p> <pre><code>alloy-host catalog add &lt;version-file.yaml&gt;\n</code></pre>"},{"location":"reference/commands/#configuration","title":"Configuration","text":""},{"location":"reference/commands/#alloy-host-config-show","title":"<code>alloy-host config show</code>","text":"<p>Show current configuration: config file path, Vagrant path, VBoxManage path.</p> <pre><code>alloy-host config show\n</code></pre>"},{"location":"reference/commands/#alloy-host-config-set-vagrant-path","title":"<code>alloy-host config set vagrant-path</code>","text":"<p>Set a custom path to the Vagrant executable.</p> <pre><code>alloy-host config set vagrant-path &lt;path&gt;\n</code></pre>"},{"location":"reference/commands/#alloy-host-config-set-vbox-manage-path","title":"<code>alloy-host config set vbox-manage-path</code>","text":"<p>Set a custom path to the VBoxManage executable.</p> <pre><code>alloy-host config set vbox-manage-path &lt;path&gt;\n</code></pre>"},{"location":"reference/commands/#alloy-host-config-unset-vagrant-path","title":"<code>alloy-host config unset vagrant-path</code>","text":"<p>Remove the custom Vagrant path and revert to auto-discovery.</p> <pre><code>alloy-host config unset vagrant-path\n</code></pre>"},{"location":"reference/commands/#alloy-host-config-unset-vbox-manage-path","title":"<code>alloy-host config unset vbox-manage-path</code>","text":"<p>Remove the custom VBoxManage path and revert to auto-discovery.</p> <pre><code>alloy-host config unset vbox-manage-path\n</code></pre>"},{"location":"reference/commands/#system","title":"System","text":""},{"location":"reference/commands/#alloy-host-check-health","title":"<code>alloy-host check-health</code>","text":"<p>Check that Vagrant and VirtualBox (VBoxManage) are installed and report their versions.</p> <pre><code>alloy-host check-health\n</code></pre>"},{"location":"reference/configuration/","title":"Configuration Reference","text":"<p>Alloy Host stores its configuration in <code>~/.alloy-it/config.yaml</code>.</p>"},{"location":"reference/configuration/#config-file","title":"Config file","text":"~/.alloy-it/config.yaml<pre><code>vagrant_path: /custom/path/to/vagrant\nvbox_manage_path: /custom/path/to/VBoxManage\n</code></pre> <p>By default both are auto-discovered from your <code>PATH</code>. Set them only if your tools are installed in a non-standard location.</p> <p>Use the CLI to manage these values:</p> <pre><code>alloy-host config show\nalloy-host config set vagrant-path /usr/local/bin/vagrant\nalloy-host config set vbox-manage-path /usr/local/bin/VBoxManage\nalloy-host config unset vagrant-path\n</code></pre>"},{"location":"reference/configuration/#file-layout","title":"File layout","text":"<pre><code>~/.alloy-it/\n\u251c\u2500\u2500 config.yaml          # global configuration\n\u251c\u2500\u2500 registry.json        # registered environments (managed automatically)\n\u2514\u2500\u2500 catalog/             # local catalog clone (populated by `catalog update`)\n</code></pre> <p>Environment directories are created wherever you ran <code>alloy-host init</code>; they are not stored inside <code>~/.alloy-it/</code>.</p>"},{"location":"reference/configuration/#registry","title":"Registry","text":"<p><code>~/.alloy-it/registry.json</code> is managed automatically. It records the name, path, blueprint, backend type, and creation time of every environment you've initialized. <code>alloy-host list</code> reads from it.</p> <p>Do not edit this file manually.</p>"},{"location":"reference/configuration/#catalog-location","title":"Catalog location","text":"<p>The local catalog is stored at <code>~/.alloy-it/catalog/</code> as a Git repository. Update it with:</p> <pre><code>alloy-host catalog update\n</code></pre> <p>To use a private or internal catalog, pass a custom remote:</p> <pre><code>alloy-host catalog update --remote https://your-org.example.com/alloy-catalog.git\n</code></pre>"},{"location":"reference/configuration/#telemetry","title":"Telemetry","text":"<p>Alloy Host supports OpenTelemetry tracing. To send traces to a collector, set the standard OTEL environment variable before running any command:</p> <pre><code>export OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318\nalloy-host up\n</code></pre>"}]}